// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, ManyToOne, OneToMany, JoinColumn, CreateDateColumn, UpdateDateColumn, DeleteDateColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { Characteristic } from './characteristic.entity';
import { EventReportingSystemRef } from './event-reporting-system-ref.entity';
import { EventSourceRef } from './event-source-ref.entity';
import { RelatedParty } from './related-party.entity';

/**
 * event with common attributes.
 */
@Entity('event')
export class Event {
  @PrimaryColumn({ type: 'varchar', length: 100 })
  id: string;

  /** The correlation id for this event. */
  @Column({ type: 'varchar', length: 100, nullable: true })
  correlationId?: string;

  /** An explnatory of the event. */
  @Column({ type: 'text', nullable: true })
  description?: string;

  /** The domain of the event. */
  @Column({ type: 'varchar', length: 255, nullable: true })
  domain?: string;

  /** The identifier of the notification. */
  @Column({ type: 'varchar', length: 100, nullable: true })
  eventId?: string;

  /** Time of the event occurrence. */
  @Column({ type: 'datetime', nullable: true })
  eventTime?: Date;

  /** The type of the notification. */
  @Column({ type: 'varchar', length: 100, nullable: true })
  eventType?: string;

  /** A priority. */
  @Column({ type: 'varchar', length: 50, nullable: true })
  priority?: string;

  /** The time the event occured. */
  @Column({ type: 'datetime', nullable: true })
  timeOcurred?: Date;

  /** The title of the event. */
  @Column({ type: 'varchar', length: 255, nullable: true })
  title?: string;

  @Column({ type: 'simple-json', nullable: true })
  event?: Record<string, any>;

  @DeleteDateColumn({ nullable: true })
  deletedAt?: Date;

  @Column({ type: 'varchar', length: 100, nullable: true })
  deletedBy?: string;

  @Column({ type: 'varchar', length: 255, nullable: true })
  deletedReason?: string;

  @CreateDateColumn()
  createdDate: Date;

  @UpdateDateColumn()
  lastUpdate: Date;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, default: 'Event' })
  atType: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  // normalised single ref: own table, FK held here
  @ManyToOne(() => EventReportingSystemRef, {
    cascade: true,
    eager: true,
    nullable: true,
  })
  @JoinColumn({ name: 'reporting_system_id' })
  reportingSystem?: EventReportingSystemRef;

  // normalised single ref: own table, FK held here
  @ManyToOne(() => EventSourceRef, {
    cascade: true,
    eager: true,
    nullable: true,
  })
  @JoinColumn({ name: 'source_id' })
  source?: EventSourceRef;

  @OneToMany(() => Characteristic, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  analyticCharacteristic: Characteristic[];

  @OneToMany(() => RelatedParty, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  relatedParty: RelatedParty[];
}
