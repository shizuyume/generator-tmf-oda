# ============================================================
# TMF736 frontend-spec baseline — generated by tmfgen fe-spec
# ------------------------------------------------------------
# Sumber IR : generator/ir-cache (hasil `tmfgen ir`)
# Base path : /tmf-api/revenueSharingAlgorithmManagement/v5
# GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
# ============================================================

meta:
  name: revenue-sharing-algorithm-portal
  label: Revenue Sharing Algorithm Management
  version: 0.1.0
  lang: id
  description: Portal manajemen Revenue Sharing Algorithm Management (TMF736)

api:
  basePath: "/tmf-api/revenueSharingAlgorithmManagement/v5"
  headers:                              # BE generated pakai ApiKeyGuard
    # KONTRAK RUNTIME: nilai secret HANYA lewat {{config.*}} (runtime gen.config),
    # TIDAK pernah lewat build-env / {{env.*}}.
    x-api-key: "{{config.TMF_API_KEY}}"
  endpoints:
    partyRevSharingAlgorithm.list: GET   /partyRevSharingAlgorithm
    partyRevSharingAlgorithm.create: POST  /partyRevSharingAlgorithm
    partyRevSharingAlgorithm.retrieve: GET   /partyRevSharingAlgorithm/{id}
    partyRevSharingAlgorithm.update: PATCH /partyRevSharingAlgorithm/{id}
    partyRevSharingAlgorithm.delete: DELETE /partyRevSharingAlgorithm/{id}
    # hub/listener (event subscription) — bukan resource CRUD;
    #   POST /hub, DELETE /hub/{id}; listener POST /listener/partyRevSharingAlgorithmAttributeValueChangeEvent, /listener/partyRevSharingAlgorithmCreateEvent, /listener/partyRevSharingAlgorithmDeleteEvent, /listener/partyRevSharingAlgorithmStateChangeEvent

output:
  # SEMENTARA full-repo: satu app utuh. Ganti ke mfe (hostApp/exposes) bila
  # target deploy adalah MFE - keputusan akhir di F2 (draf fe-generators).
  type: full-repo
  fullRepo:
    router: react-router
    state: context
    packageManager: npm

ui:
  library: mui
  version: "^9.0.0"              # mui WAJIB ^9.x (normalisasi validator v1)
  theme:
    name: indigo
    darkMode: true
    tokens:
      primary: "#2563eb"
      surface: "#ffffff"
      background: "#f8fafc"
      text: "#0f172a"
      textMuted: "#64748b"
      radius: 8
      font: Inter

formEngine:
  library: react-hook-form
  version: "^7.0.0"
  validator: zod
  validatorVersion: "^3.0.0"
  defaultMode: onChange
  defaultReValidateMode: onChange
  behaviour:
    shouldUnregister: true
    resetOnSubmit: false

layout:
  shell: sidebar-topbar
  sidebar:
    collapsible: true
    itemsSource: from-pages
  topbar:
    search: false
    themeToggle: true
  footer: false

auth:
  # KONTRAK: BE generated memakai ApiKeyGuard (x-api-key via api.headers),
  # BUKAN login session/JWT. enabled tetap false sampai ada auth server.
  enabled: false

# ============================================================
# PAGES
# ============================================================
pages:
  # ========================================================
  # PARTY REV SHARING ALGORITHM — list + add/edit modal + detail
  # ========================================================
  - id: partyRevSharingAlgorithm
    path: "/partyRevSharingAlgorithms"
    title: Party Rev Sharing Algorithm
    icon: Percent
    nav:
      parent: top

    actions:
      - label: Tambah Party Rev Sharing Algorithm
        variant: primary
        position: top-right
        action: open-form
        form: add
        icon: Plus

    view: list
    list:
      api: "{{api.basePath}}/partyRevSharingAlgorithm"
      method: GET
      queryParams:
        fields: ""
        offset: "{{pagination.offset}}"
        limit: "{{pagination.limit}}"
        q: "{{search.q}}"
      search:
        enabled: true
        mode: server-side
        debounceMs: 300
        placeholder: Cari nama / deskripsi...
        fields: [name, description]
      columns:
        - field: name
          label: Nama
          sortable: true
        - field: description
          label: Deskripsi
          render: truncated
        - field: policy
          label: Policy
          render: tag-count
        - field: actions
          label: ""
          type: row-actions
          items:
            - { label: Detail, icon: Eye, action: open-detail }
            - { label: Edit, icon: Pencil, action: open-form, form: edit }
            - { label: Hapus, icon: Trash2, action: delete, confirm: true }
      pagination:
        mode: server-side
        scheme: offset-limit
        perPageOptions: [10, 25, 50]
      emptyState:
        title: Belum ada Party Rev Sharing Algorithm
        action: { label: Tambah Party Rev Sharing Algorithm, open: add }

    forms:
      add:
        mode: modal
        title: Tambah Party Rev Sharing Algorithm
        api:
          method: POST
          path: "{{api.basePath}}/partyRevSharingAlgorithm"
          payload:
            "@type": fields.atType
            name: fields.name
            description: fields.description
            policy: fields.policy
            conditionVariable: fields.conditionVariable
            actionVariable: fields.actionVariable
        fields:
          - name: atType
            label: "@type"
            type: text
            readOnly: true
            defaultValue: PartyRevSharingAlgorithm
            required: true
          - name: name
            label: Nama
            type: text
            required: true
          - name: description
            label: Deskripsi
            type: textarea
            rows: 3
          - name: policy
            label: Policy
            type: repeatable
            minItems: 1
            itemLabel: "Policy #{{index}}"
            addButton: "+ Tambah Policy"
            itemPayload:
              "@type": item.atType
              "@referredType": item.referredType
              href: item.href
              id: item.id
              name: item.name
              version: item.version
            itemFields:
              - name: atType
                type: text
                readOnly: true
                defaultValue: PolicyRef
              - name: referredType
                label: "@referredType"
                type: select
                options: { PolicySet: PolicySet }
                defaultValue: PolicySet
              - name: href
                label: Href
                type: text
                readOnly: true
                computedFrom: id
              - name: id
                label: Policy ID
                type: text
                required: true
              - name: name
                label: Nama
                type: text
              - name: version
                label: Version
                type: text
          - name: conditionVariable
            label: Condition Variable
            type: repeatable
            itemLabel: "Condition Variable #{{index}}"
            addButton: "+ Tambah Condition Variable"
            itemPayload:
              "@type": item.atType
              value: item.value
              policyCondition:
                "@type": PolicyConditionRef
                href: item.policyConditionHref
                id: item.policyConditionId
                name: item.policyConditionName
                "@referredType": item.policyConditionReferredType
              policyConditionVariable:
                "@type": PolicyVariableRef
                href: item.policyConditionVariableHref
                id: item.policyConditionVariableId
                name: item.policyConditionVariableName
                "@referredType": item.policyConditionVariableReferredType
            itemFields:
              - name: atType
                type: text
                readOnly: true
                defaultValue: PartyRevSharingPolicyConditionVariable
              - name: value
                label: Value
                type: text
              - name: policyConditionId
                label: Policy Condition ID
                type: text
              - name: policyConditionName
                label: Policy Condition
                type: text
              - name: policyConditionReferredType
                label: "@referredType"
                type: select
                options: { PolicyCondition: PolicyCondition }
                defaultValue: PolicyCondition
              - name: policyConditionVariableId
                label: Policy Condition Variable ID
                type: text
              - name: policyConditionVariableName
                label: Policy Condition Variable
                type: text
              - name: policyConditionVariableReferredType
                label: "@referredType"
                type: select
                options: { PolicyVariable: PolicyVariable }
                defaultValue: PolicyVariable
          - name: actionVariable
            label: Action Variable
            type: repeatable
            itemLabel: "Action Variable #{{index}}"
            addButton: "+ Tambah Action Variable"
            itemPayload:
              "@type": item.atType
              value: item.value
              policyAction:
                "@type": PolicyActionRef
                href: item.policyActionHref
                id: item.policyActionId
                name: item.policyActionName
                "@referredType": item.policyActionReferredType
              policyActionVariable:
                "@type": PolicyVariableRef
                href: item.policyActionVariableHref
                id: item.policyActionVariableId
                name: item.policyActionVariableName
                "@referredType": item.policyActionVariableReferredType
            itemFields:
              - name: atType
                type: text
                readOnly: true
                defaultValue: PartyRevSharingPolicyActionVariable
              - name: value
                label: Value
                type: text
              - name: policyActionId
                label: Policy Action ID
                type: text
              - name: policyActionName
                label: Policy Action
                type: text
              - name: policyActionReferredType
                label: "@referredType"
                type: select
                options: { PolicyAction: PolicyAction }
                defaultValue: PolicyAction
              - name: policyActionVariableId
                label: Policy Action Variable ID
                type: text
              - name: policyActionVariableName
                label: Policy Action Variable
                type: text
              - name: policyActionVariableReferredType
                label: "@referredType"
                type: select
                options: { PolicyVariable: PolicyVariable }
                defaultValue: PolicyVariable
        buttons:
          submit: { label: Simpan, position: footer-right, variant: primary }
          cancel: { label: Batal, variant: ghost }
      edit:
        mode: modal
        title: Edit Party Rev Sharing Algorithm
        prefillFrom: detail
        api:
          method: PATCH
          path: "{{api.basePath}}/partyRevSharingAlgorithm/{id}"
          payload:
            name: fields.name
            description: fields.description
            policy: fields.policy
            conditionVariable: fields.conditionVariable
            actionVariable: fields.actionVariable
        fields: [ reuse: add ]
        buttons:
          submit: { label: Simpan, position: footer-right, variant: primary }
          cancel: { label: Batal, variant: ghost }

    detail:
      mode: page
      path: "/partyRevSharingAlgorithms/:id"
      api: "{{api.basePath}}/partyRevSharingAlgorithm/{id}"
      header:
        title: "{{partyRevSharingAlgorithm.name}}"
        subtitle: "{{partyRevSharingAlgorithm.id}}"
        backButton: true
        actions:
          - { label: Edit, action: open-form, form: edit }
          - { label: Hapus, variant: danger, action: delete, confirm: true }
      sections:
        - type: tabs
          tabs:
            - label: Ringkasan
              blocks:
              - type: card
                layout: description-list
                fields:
                  - { field: name, label: Nama }
                  - { field: description, label: Deskripsi }
                  - { field: "@type", label: "@type" }
                  - { field: id, label: ID, mono: true }
                  - { field: href, label: Href, render: link }
            - label: Policy
              blocks:
              - type: table
                field: policy
                columns: ["@referredType", href, id, name, version]
                emptyState: { title: Belum ada Policy }
            - label: Condition Variable
              blocks:
              - type: table
                field: conditionVariable
                columns: [value]
                renderColumns:
                  - { field: policyCondition, sub: name, label: Policy Condition }
                  - { field: policyConditionVariable, sub: name, label: Policy Condition Variable }
                emptyState: { title: Belum ada Condition Variable }
            - label: Action Variable
              blocks:
              - type: table
                field: actionVariable
                columns: [value]
                renderColumns:
                  - { field: policyAction, sub: name, label: Policy Action }
                  - { field: policyActionVariable, sub: name, label: Policy Action Variable }
                emptyState: { title: Belum ada Action Variable }

