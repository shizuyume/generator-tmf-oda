// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, OneToMany, CreateDateColumn, UpdateDateColumn, DeleteDateColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { CalendarPeriod } from './calendar-period.entity';
import { GeographicSiteRelationship } from './geographic-site-relationship.entity';
import { PlaceRefOrValue } from './place-ref-or-value.entity';
import { RelatedParty } from './related-party.entity';

@Entity('geographic_site')
export class GeographicSite {
  @PrimaryColumn({ type: 'varchar', length: 36 })
  id: string;

  /** A code that may be used for some addressing schemes eg: [ANSI T1.253-1999] */
  @Column({ type: 'varchar', length: 255, nullable: true })
  code?: string;

  /** Text describing additional information regarding the site */
  @Column({ type: 'text', nullable: true })
  description?: string;

  /** A user-friendly name for the place, such as [Paris Store], [London Store], [Main Home] */
  @Column({ type: 'varchar', length: 255, nullable: true })
  name?: string;

  /** The condition of the GeographicSite, such as planned, underConstruction, cancelled, active, inactive, former */
  @Column({ type: 'varchar', length: 50, nullable: true })
  status?: string;

  @DeleteDateColumn({ nullable: true })
  deletedAt?: Date;

  @Column({ type: 'varchar', length: 100, nullable: true })
  deletedBy?: string;

  @Column({ type: 'varchar', length: 255, nullable: true })
  deletedReason?: string;

  @CreateDateColumn()
  createdDate: Date;

  @UpdateDateColumn()
  lastUpdate: Date;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, default: 'GeographicSite' })
  atType: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  @OneToMany(() => CalendarPeriod, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  calendar: CalendarPeriod[];

  @OneToMany(() => PlaceRefOrValue, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  place: PlaceRefOrValue[];

  @OneToMany(() => RelatedParty, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  relatedParty: RelatedParty[];

  @OneToMany(() => GeographicSiteRelationship, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  siteRelationship: GeographicSiteRelationship[];
}
