// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, ManyToOne, OneToMany, JoinColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { GeographicSite } from './geographic-site.entity';
import { HourPeriod } from './hour-period.entity';

@Entity('calendar_period')
export class CalendarPeriod {
  @PrimaryColumn({ type: 'varchar', length: 100 })
  id: string;

  /** The id the client gave this item. Unique within its parent only, so it is not the primary key; served back as `id`. */
  @Column({ type: 'varchar', length: 100, nullable: true })
  refId?: string;

  @Column({ type: 'int', nullable: true })
  sortOrder?: number;

  /** Day where the calendar status applies (e.g.: monday, mon-to-fri, weekdays, weekend, all week, ...) */
  @Column({ type: 'varchar', length: 255, nullable: true })
  day?: string;

  /** Indication of the availability of the site (e.g.: open) */
  @Column({ type: 'varchar', length: 50, nullable: true })
  status?: string;

  /** Indication of the timezone applicable to the calendar information (e.g.: Paris, GMT+1) */
  @Column({ type: 'varchar', length: 255, nullable: true })
  timeZone?: string;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atType?: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  @ManyToOne(() => GeographicSite, (parent) => parent.calendar, {
    onDelete: 'CASCADE',
    nullable: false,
  })
  @JoinColumn({ name: 'geographic_site_id' })
  owner: GeographicSite;

  @OneToMany(() => HourPeriod, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  hourPeriod: HourPeriod[];
}
