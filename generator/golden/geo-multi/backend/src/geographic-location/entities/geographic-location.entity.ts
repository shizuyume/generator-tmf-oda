// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, OneToMany, CreateDateColumn, UpdateDateColumn, DeleteDateColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { GeographicPoint } from './geographic-point.entity';

/**
 * Structured textual way of describing a geographic location. A geographic location allows describing through coordinate(s), a point, a line or a space.
 */
@Entity('geographic_location')
export class GeographicLocation {
  @PrimaryColumn({ type: 'varchar', length: 100 })
  id: string;

  /** Name of the geo location */
  @Column({ type: 'varchar', length: 255 })
  name: string;

  /** Type of the geographic location - one of: point, line, graph, ring polygon */
  @Column({ type: 'varchar', length: 100 })
  geometryType: string;

  /** Geocoding referential */
  @Column({ type: 'varchar', length: 255 })
  spatialRef: string;

  /** Accuracy of the coordinate specified */
  @Column({ type: 'varchar', length: 255 })
  accuracy: string;

  @DeleteDateColumn({ nullable: true })
  deletedAt?: Date;

  @Column({ type: 'varchar', length: 100, nullable: true })
  deletedBy?: string;

  @Column({ type: 'varchar', length: 255, nullable: true })
  deletedReason?: string;

  @CreateDateColumn()
  createdDate: Date;

  @UpdateDateColumn()
  lastUpdate: Date;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, default: 'GeographicLocation' })
  atType: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  @OneToMany(() => GeographicPoint, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  geometry: GeographicPoint[];
}
