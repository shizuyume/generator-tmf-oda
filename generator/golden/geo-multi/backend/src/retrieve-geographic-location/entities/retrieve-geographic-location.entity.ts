// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, ManyToOne, JoinColumn, CreateDateColumn, UpdateDateColumn, DeleteDateColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { GeographicLocationReferred } from './geographic-location-referred.entity';
import { RetrieveGeographicLocationThing } from './retrieve-geographic-location-thing.entity';

/**
 * This resource is used to manage the request and response to retrieve a thing (any object)
 */
@Entity('retrieve_geographic_location')
export class RetrieveGeographicLocation {
  @PrimaryColumn({ type: 'varchar', length: 36 })
  id: string;

  /** Status of the task (inProgress, terminatedWithErrror, done) */
  @Column({ type: 'varchar', length: 50, nullable: true })
  status?: string;

  /** Requested accuracy of the retrieval */
  @Column({ type: 'float', nullable: true })
  requestedAccuracy?: number;

  /** Timestamp of end of task execution */
  @Column({ type: 'datetime', nullable: true })
  time?: Date;

  @DeleteDateColumn({ nullable: true })
  deletedAt?: Date;

  @Column({ type: 'varchar', length: 100, nullable: true })
  deletedBy?: string;

  @Column({ type: 'varchar', length: 255, nullable: true })
  deletedReason?: string;

  @CreateDateColumn()
  createdDate: Date;

  @UpdateDateColumn()
  lastUpdate: Date;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, default: 'RetrieveGeographicLocation' })
  atType: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  // normalised single ref: own table, FK held here
  @ManyToOne(() => RetrieveGeographicLocationThing, {
    cascade: true,
    eager: true,
    nullable: true,
  })
  @JoinColumn({ name: 'thing_id' })
  thing?: RetrieveGeographicLocationThing;

  // normalised single ref: own table, FK held here
  @ManyToOne(() => GeographicLocationReferred, {
    cascade: true,
    eager: true,
    nullable: true,
  })
  @JoinColumn({ name: 'geographic_location_id' })
  geographicLocation?: GeographicLocationReferred;
}
