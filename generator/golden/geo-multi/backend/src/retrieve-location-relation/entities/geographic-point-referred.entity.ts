// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, ManyToOne, JoinColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { RetrieveLocationRelationIntersection } from './retrieve-location-relation-intersection.entity';

@Entity('geographic_point_referred')
export class GeographicPointReferred {
  @PrimaryColumn({ type: 'varchar', length: 36 })
  id: string;

  /** x coordinate (usually latitude) */
  @Column({ type: 'varchar', length: 255 })
  x: string;

  /** y coordinate (usually longitude) */
  @Column({ type: 'varchar', length: 255 })
  y: string;

  /** z coordinate (usually elevation) */
  @Column({ type: 'varchar', length: 255, nullable: true })
  z?: string;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atType?: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  @ManyToOne(() => RetrieveLocationRelationIntersection, (parent) => parent.geometry, {
    onDelete: 'CASCADE',
    nullable: false,
  })
  @JoinColumn({ name: 'retrieve_location_relation_intersection_id' })
  owner: RetrieveLocationRelationIntersection;
}
