// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, OneToMany, CreateDateColumn, UpdateDateColumn, DeleteDateColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { GeographicLocationRef } from './geographic-location-ref.entity';
import { RetrieveLocationRelationIntersection } from './retrieve-location-relation-intersection.entity';

/**
 * This resource is used to manage the request and response to retrieve the relation between two geographic locations
 */
@Entity('retrieve_location_relation')
export class RetrieveLocationRelation {
  @PrimaryColumn({ type: 'varchar', length: 36 })
  id: string;

  /** Status of the task (inProgress, terminatedWithErrror, done) */
  @Column({ type: 'varchar', length: 50, nullable: true })
  status?: string;

  /** Calculated distance between location A and location B */
  @Column({ type: 'varchar', length: 255 })
  distance: string;

  /** Timestamp of end of task execution */
  @Column({ type: 'datetime' })
  time: Date;

  @DeleteDateColumn({ nullable: true })
  deletedAt?: Date;

  @Column({ type: 'varchar', length: 100, nullable: true })
  deletedBy?: string;

  @Column({ type: 'varchar', length: 255, nullable: true })
  deletedReason?: string;

  @CreateDateColumn()
  createdDate: Date;

  @UpdateDateColumn()
  lastUpdate: Date;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, default: 'RetrieveLocationRelation' })
  atType: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  @OneToMany(() => GeographicLocationRef, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  geographicLocation: GeographicLocationRef[];

  @OneToMany(() => RetrieveLocationRelationIntersection, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  intersection: RetrieveLocationRelationIntersection[];
}
