// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import {
  Controller,
  Post,
  Body,
  Logger,
  HttpCode,
  HttpStatus,
} from '@nestjs/common';
import { ApiTags } from '@nestjs/swagger';
import { Repository } from 'typeorm';
import { InjectRepository } from '@nestjs/typeorm';
import { TMF674_BASE_PATH } from '../common/constants/tmf.constants';
import { EventSubscription } from '../subscription/entities/event-subscription.entity';

// Publish-only architecture: these endpoints are stubs that log and echo, matching
// the reference. Nothing is consumed here.
@ApiTags('listener')
@Controller(`${TMF674_BASE_PATH}/listener`)
export class GeographicSiteListenerController {
  private readonly logger = new Logger(GeographicSiteListenerController.name);

  constructor(
    @InjectRepository(EventSubscription)
    private readonly subscriptionRepository: Repository<EventSubscription>,
  ) {}

  private async findSubscription(body: any): Promise<EventSubscription> {
    if (body?.subscriptionId) {
      const sub = await this.subscriptionRepository.findOne({
        where: { id: body.subscriptionId },
      });
      if (sub) return sub;
    }
    const [latest] = await this.subscriptionRepository.find({
      order: { createdDate: 'DESC' },
      take: 1,
    });
    return (
      latest ||
      ({
        id: 'unknown',
        callback: '',
        createdDate: new Date(),
      } as unknown as EventSubscription)
    );
  }

  @Post('geographicSiteAttributeValueChangeEvent')
  @HttpCode(HttpStatus.CREATED)
  async handleGeographicSiteAttributeValueChangeEvent(@Body() body: any) {
    this.logger.log('Received geographicSiteAttributeValueChangeEvent');
    return this.findSubscription(body);
  }

  @Post('geographicSiteCreateEvent')
  @HttpCode(HttpStatus.CREATED)
  async handleGeographicSiteCreateEvent(@Body() body: any) {
    this.logger.log('Received geographicSiteCreateEvent');
    return this.findSubscription(body);
  }

  @Post('geographicSiteDeleteEvent')
  @HttpCode(HttpStatus.CREATED)
  async handleGeographicSiteDeleteEvent(@Body() body: any) {
    this.logger.log('Received geographicSiteDeleteEvent');
    return this.findSubscription(body);
  }

  @Post('geographicSiteStatusChangeEvent')
  @HttpCode(HttpStatus.CREATED)
  async handleGeographicSiteStatusChangeEvent(@Body() body: any) {
    this.logger.log('Received geographicSiteStatusChangeEvent');
    return this.findSubscription(body);
  }
}
