// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
/**
 * Event
 *
 * The generated service and controller, against mocked repositories.
 *
 * TypeORM is mocked rather than run: a service emitted with --db postgres
 * carries postgres column types and will not boot on sqlite, so an in-memory
 * database is not an option.
 *
 * Every literal below is derived from this resource, never hand-written: see
 * the mapping table in emit/spec/resource.mjs for where each one comes from.
 */
import { EventService } from '../../src/tmf-event/event.service';
import { EventController } from '../../src/tmf-event/event.controller';
import { EventEventType } from '../../src/event/event-types';
import { Characteristic } from '../../src/tmf-event/entities/characteristic.entity';
import { CharacteristicRelationship } from '../../src/tmf-event/entities/characteristic-relationship.entity';
import { Event } from '../../src/tmf-event/entities/event.entity';
import { EventReportingSystemRef } from '../../src/tmf-event/entities/event-reporting-system-ref.entity';
import { EventSourceRef } from '../../src/tmf-event/entities/event-source-ref.entity';
import { RelatedParty } from '../../src/tmf-event/entities/related-party.entity';

type AnyRec = Record<string, any>;

function makeRepo(): AnyRec {
  return {
    create: jest.fn(() => ({}) as AnyRec),
    save: jest.fn(async (e: AnyRec) => e),
    findOne: jest.fn(),
    createQueryBuilder: jest.fn(),
  };
}

function makeQueryBuilder(rows: AnyRec[]): AnyRec {
  const qb: AnyRec = {
    leftJoinAndSelect: jest.fn(() => qb),
    where: jest.fn(() => qb),
    andWhere: jest.fn(() => qb),
    addOrderBy: jest.fn(() => qb),
    skip: jest.fn(() => qb),
    take: jest.fn(() => qb),
    getCount: jest.fn(async () => rows.length),
    getMany: jest.fn(async () => rows),
  };
  return qb;
}

const HREF = '/tmf-api/event/v4/event/res-1';

/** The owner-bound collections this resource maps. */
const COLLECTIONS = ['analyticCharacteristic', 'relatedParty'] as const;

/** The path segment each collection's row mapper writes into its href. */
const HREF_SEGMENT: Record<string, string> = {
  analyticCharacteristic: 'analyticCharacteristic',
  relatedParty: 'relatedParty',
};

/** The `@type` each collection's rows carry: their own entity class name. */
const ROW_AT_TYPE: Record<string, string> = {
  analyticCharacteristic: 'Characteristic',
  relatedParty: 'RelatedParty',
};

function row(collection: string, index: number): AnyRec {
  return {
    id: `${collection}-row-${index}`,
    refId: `${collection}-ref-${index}`,
    atType: ROW_AT_TYPE[collection],
    sortOrder: index,
  };
}

function entity(over: AnyRec = {}): AnyRec {
  return {
    id: 'res-1',
    correlationId: 'res-correlationId',
    description: 'res-description',
    domain: 'res-domain',
    eventId: 'res-eventId',
    eventTime: 'res-eventTime',
    eventType: 'res-eventType',
    priority: 'res-priority',
    timeOcurred: 'res-timeOcurred',
    title: 'res-title',
    event: 'res-event',
    atType: 'Event',
    atSchemaLocation: 'res-atSchemaLocation',
    atBaseType: 'res-atBaseType',
    analyticCharacteristic: [row('analyticCharacteristic', 0)],
    relatedParty: [row('relatedParty', 0)],
    ...over,
  };
}

describe('Event entities', () => {
  it('are constructible, so the mocked repositories stand in for real rows', () => {
    for (const E of [
      Characteristic,
      CharacteristicRelationship,
      Event,
      EventReportingSystemRef,
      EventSourceRef,
      RelatedParty,
    ]) {
      expect(new (E as new () => object)()).toBeInstanceOf(E as never);
    }
  });
});
