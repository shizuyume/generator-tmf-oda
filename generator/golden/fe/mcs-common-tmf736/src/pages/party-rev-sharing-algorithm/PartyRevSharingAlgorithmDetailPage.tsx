// GENERATED by fe-gen emit mcs-common - DO NOT EDIT. Re-run `fe-gen emit` instead.
import { useCallback, useEffect, useState } from 'react';
import CustomBreadcrumbs from 'common_remote/Breadcrumbs';
import CustomButton from 'common_remote/Button';
import CustomDialog from 'common_remote/Dialog';
import CustomSnackbar from 'common_remote/Snackbar';
import CustomTypography from 'common_remote/Typography';
import Box from '@mui/material/Box';
import Stack from '@mui/material/Stack';
import Table from '@mui/material/Table';
import TableBody from '@mui/material/TableBody';
import TableCell from '@mui/material/TableCell';
import TableContainer from '@mui/material/TableContainer';
import TableHead from '@mui/material/TableHead';
import TableRow from '@mui/material/TableRow';
import PillTabs from 'common_remote/PillTabs';
import {
  DetailField,
  SectionCard,
  DETAIL_GRID_SX,
  ViewToggle,
  ViewMode,
} from '../../components/DetailComponents';
import CreatePartyRevSharingAlgorithmDialog from './CreatePartyRevSharingAlgorithmDialog';
import { buildPayload, mapDetailToDefaults } from '../../lib/buildPayload';
import type { ComputedFromRule } from '../../lib/buildPayload';
import { getPartyRevSharingAlgorithm, deletePartyRevSharingAlgorithm, patchPartyRevSharingAlgorithm, getErrorMessage } from '../../service';
import type { PartyRevSharingAlgorithm } from '../../types';

type SnackState = { open: boolean; message: string; severity: 'success' | 'error' };

// TODO: CUSTOMIZE — per-resource template point (endpoint/fields differ per TMF component).
const EDIT_PAYLOAD_SPEC = {
  "name": "fields.name",
  "description": "fields.description",
  "policy": "fields.policy",
  "conditionVariable": "fields.conditionVariable",
  "actionVariable": "fields.actionVariable"
};
const EDIT_REPEATABLE_ITEM_PAYLOADS = {
  "policy": {
    "@type": "item.atType",
    "id": "item.id",
    "href": "item.href",
    "name": "item.name",
    "@referredType": "item.referredType",
    "version": "item.version"
  },
  "conditionVariable": {
    "@type": "item.atType",
    "value": "item.value",
    "policyCondition": {
      "@type": "PolicyConditionRef",
      "id": "item.policyConditionId",
      "href": "item.policyConditionHref",
      "name": "item.policyConditionName",
      "@referredType": "item.policyConditionReferredType"
    },
    "policyConditionVariable": {
      "@type": "PolicyVariableRef",
      "id": "item.policyConditionVariableId",
      "name": "item.policyConditionVariableName",
      "@referredType": "item.policyConditionVariableReferredType"
    }
  },
  "actionVariable": {
    "@type": "item.atType",
    "value": "item.value",
    "policyAction": {
      "@type": "PolicyActionRef",
      "id": "item.policyActionId",
      "href": "item.policyActionHref",
      "name": "item.policyActionName",
      "@referredType": "item.policyActionReferredType"
    },
    "policyActionVariable": {
      "@type": "PolicyVariableRef",
      "id": "item.policyActionVariableId",
      "name": "item.policyActionVariableName",
      "@referredType": "item.policyActionVariableReferredType"
    }
  }
};
const EDIT_COMPUTED_FROM_ROOT: ComputedFromRule[] = [];
const EDIT_COMPUTED_FROM_REPEATABLE: Record<string, ComputedFromRule[]> = {
  "policy": [
    {
      "target": "href",
      "source": "id"
    }
  ]
};

interface Props {
  id: string;
  onBack: () => void;
}

export default function PartyRevSharingAlgorithmDetailPage({ id, onBack }: Readonly<Props>) {
  const [data, setData] = useState<PartyRevSharingAlgorithm | null>(null);
  const [loading, setLoading] = useState(true);
  const [activeTab, setActiveTab] = useState(0);
  const [snackbar, setSnackbar] = useState<SnackState>({ open: false, message: '', severity: 'success' });
  const [confirmDelete, setConfirmDelete] = useState(false);
  const [editOpen, setEditOpen] = useState(false);
  const [savingEdit, setSavingEdit] = useState(false);
  const [policyView, setPolicyView] = useState<ViewMode>('card');
  const [conditionVariablesView, setConditionVariablesView] = useState<ViewMode>('card');
  const [actionVariablesView, setActionVariablesView] = useState<ViewMode>('card');

  const loadDetail = useCallback(async () => {
    setLoading(true);
    try {
      const result = await getPartyRevSharingAlgorithm(id);
      setData(result);
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    } finally {
      setLoading(false);
    }
  }, [id]);

  useEffect(() => { loadDetail(); }, [loadDetail]);

  const handleDelete = async () => {
    try {
      await deletePartyRevSharingAlgorithm(id);
      setSnackbar({ open: true, message: 'Deleted successfully', severity: 'success' });
      setConfirmDelete(false);
      onBack();
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    }
  };
  const handleEditSave = async (values: Record<string, any>) => {
    setSavingEdit(true);
    try {
      const payload = buildPayload(values, EDIT_PAYLOAD_SPEC, EDIT_REPEATABLE_ITEM_PAYLOADS, EDIT_COMPUTED_FROM_ROOT, EDIT_COMPUTED_FROM_REPEATABLE);
      await patchPartyRevSharingAlgorithm(id, payload as any);
      setSnackbar({ open: true, message: 'Saved successfully', severity: 'success' });
      setEditOpen(false);
      await loadDetail();
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    } finally {
      setSavingEdit(false);
    }
  };

  if (loading) {
    return <Box sx={{ p: 3 }}><CustomTypography>Loading...</CustomTypography></Box>;
  }
  if (!data) {
    return (
      <Box sx={{ p: 3 }}>
        <CustomButton label="← Back" variant="text" onClick={onBack} />
        <CustomTypography color="error">Not found</CustomTypography>
      </Box>
    );
  }

  const tab0Content = (
    <>
      <SectionCard>
          <Box sx={DETAIL_GRID_SX}>
            <DetailField label={"Nama"} value={data.name} />
            <DetailField label={"Deskripsi"} value={data.description} />
            <DetailField label={"@type"} value={data["@type"]} />
            <DetailField label={"ID"} value={data.id} />
            <DetailField label={"Href"} value={data.href ? <a href={data.href}>{data.href}</a> : undefined} />
          </Box>
        </SectionCard>
    </>
  );

  const tab1Content = (
    <>
      {(() => { const policyItems = data.policy ?? []; return (
        <SectionCard title={"Policy"} action={<ViewToggle value={policyView} onChange={setPolicyView} />}>
          {(policyItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada policy</CustomTypography>
          ) : policyView === 'card' ? (
            <Stack spacing={1.5}>
              {(policyItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"id"} value={item["id"]} />
                    <DetailField label={"name"} value={item["name"]} />
                    <DetailField label={"@referredType"} value={item["@referredType"]} />
                    <DetailField label={"version"} value={item["version"]} />
                    <DetailField label={"href"} value={item["href"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>id</TableCell>
                    <TableCell>name</TableCell>
                    <TableCell>@referredType</TableCell>
                    <TableCell>version</TableCell>
                    <TableCell>href</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(policyItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["id"] ?? '-'}</TableCell>
                      <TableCell>{row["name"] ?? '-'}</TableCell>
                      <TableCell>{row["@referredType"] ?? '-'}</TableCell>
                      <TableCell>{row["version"] ?? '-'}</TableCell>
                      <TableCell>{row["href"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tab2Content = (
    <>
      {(() => { const conditionVariableItems = data.conditionVariable ?? []; return (
        <SectionCard title={"Condition Variables"} action={<ViewToggle value={conditionVariablesView} onChange={setConditionVariablesView} />}>
          {(conditionVariableItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada condition variable</CustomTypography>
          ) : conditionVariablesView === 'card' ? (
            <Stack spacing={1.5}>
              {(conditionVariableItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"value"} value={item["value"]} />
                    <DetailField label={"Policy Condition"} value={item["policyCondition"]?.["name"]} />
                    <DetailField label={"Policy Variable"} value={item["policyConditionVariable"]?.["name"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>value</TableCell>
                    <TableCell>Policy Condition</TableCell>
                    <TableCell>Policy Variable</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(conditionVariableItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["value"] ?? '-'}</TableCell>
                      <TableCell>{row["policyCondition"]?.["name"] ?? '-'}</TableCell>
                      <TableCell>{row["policyConditionVariable"]?.["name"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tab3Content = (
    <>
      {(() => { const actionVariableItems = data.actionVariable ?? []; return (
        <SectionCard title={"Action Variables"} action={<ViewToggle value={actionVariablesView} onChange={setActionVariablesView} />}>
          {(actionVariableItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada action variable</CustomTypography>
          ) : actionVariablesView === 'card' ? (
            <Stack spacing={1.5}>
              {(actionVariableItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"value"} value={item["value"]} />
                    <DetailField label={"Policy Action"} value={item["policyAction"]?.["name"]} />
                    <DetailField label={"Policy Variable"} value={item["policyActionVariable"]?.["name"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>value</TableCell>
                    <TableCell>Policy Action</TableCell>
                    <TableCell>Policy Variable</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(actionVariableItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["value"] ?? '-'}</TableCell>
                      <TableCell>{row["policyAction"]?.["name"] ?? '-'}</TableCell>
                      <TableCell>{row["policyActionVariable"]?.["name"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tabs = [
    { label: "Ringkasan", content: tab0Content },
    { label: "Policy", content: tab1Content },
    { label: "Condition Variables", content: tab2Content },
    { label: "Action Variables", content: tab3Content }
  ];

  return (
    <Box sx={{ p: 3 }}>
      <CustomBreadcrumbs items={[{ label: "Party Rev Sharing Algorithms", onClick: onBack }, { label: (data as any).name ?? data.id }]} />

      <Stack direction="row" spacing={1.25} sx={{ mt: 2 }}>
        <CustomButton label="← Back to List" onClick={onBack} variant="outlined" />
        <CustomButton label="Edit" onClick={() => setEditOpen(true)} variant="contained" />
        <CustomButton label="Delete" onClick={() => setConfirmDelete(true)} variant="contained" color="error" />
      </Stack>

      <Box sx={{ mt: 2, mb: 1 }}>
        <CustomTypography variant="h5">{(data as any).name ?? data.id}</CustomTypography>
      </Box>

      <Box sx={{ mt: 2 }}>
        <PillTabs tabs={tabs} value={activeTab} onChange={setActiveTab} />
      </Box>

      <CustomDialog
        open={confirmDelete}
        onClose={() => setConfirmDelete(false)}
        title="Delete"
        content={<CustomTypography>Are you sure you want to delete this record?</CustomTypography>}
        actions={
          <Stack direction="row" spacing={1}>
            <CustomButton label="Cancel" variant="outlined" onClick={() => setConfirmDelete(false)} />
            <CustomButton label="Delete" variant="contained" color="error" onClick={handleDelete} />
          </Stack>
        }
      />

      {editOpen && (
        <CreatePartyRevSharingAlgorithmDialog
          open={editOpen}
          onClose={() => setEditOpen(false)}
          onSave={handleEditSave}
          saving={savingEdit}
          initialValues={mapDetailToDefaults(data as unknown as Record<string, any>, EDIT_PAYLOAD_SPEC, EDIT_REPEATABLE_ITEM_PAYLOADS)}
          title="Edit Algoritma"
          submitLabel="Save"
        />
      )}

      <CustomSnackbar open={snackbar.open} onClose={() => setSnackbar((s) => ({ ...s, open: false }))} message={snackbar.message} severity={snackbar.severity} />
    </Box>
  );
}
