// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Dashboard overview: 2 stat, 1 chart (placeholder), 1 tabel.
// Data stats/tables dari apis[] registry (F5) - fetch sekali saat mount, tanpa polling.

import { ReactElement, useEffect, useState } from 'react';
import { apiRequest } from '../../gen/apiClient';
import { UiBox, UiTypography, UiCard, UiTable, UiTableHead, UiTableBody, UiTableRow, UiTableCell, UiChip, UiStatCard } from '../../gen/ui';

export default function Dashboard(): ReactElement {
  const [data, setData] = useState<Record<string, unknown>>({});
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    let alive = true;
    Promise.all([
      apiRequest('/api/inventory/stats/summary', { method: 'GET' }).then((e) => ['statsSummary', e.data] as const),
      apiRequest('/api/inventory/items', { method: 'GET' }).then((e) => ['listItems', e.data] as const),
    ]).then((entries) => {
      if (!alive) return;
      setData(Object.fromEntries(entries));
    }).catch(() => {
      // Backend belum tersedia / salah satu request gagal - dashboard tetap
      // tampil dengan data kosong (stat 0, tabel kosong), BUKAN crash. Sama pola
      // dengan empty-state list/detail lain - tanpa ini Promise.all yang reject
      // tak pernah tertangkap sama sekali (unhandled rejection -> React crash).
    }).finally(() => {
      if (alive) setLoading(false);
    });
    return () => { alive = false; };
  }, []);

  if (loading) {
    return (<UiBox sx={{ p: 4 }}><UiTypography>Memuat...</UiTypography></UiBox>);
  }

  return (
    <UiBox sx={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
      <UiBox sx={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: 2 }}>
        <UiStatCard label={'Total Item'} value={new Intl.NumberFormat('id-ID').format(Number((data['statsSummary'] as Record<string, unknown> | undefined)?.['total']) || 0)} />
        <UiStatCard label={'Nilai Total'} value={new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(Number((data['statsSummary'] as Record<string, unknown> | undefined)?.['totalValue']) || 0)} />
      </UiBox>
      <UiCard sx={{ p: 2 }}>
        <UiTypography variant="body2" sx={{ color: 'text.secondary' }}>{'Tren Item'}</UiTypography>
        <UiTypography variant="caption" sx={{ color: 'text.secondary' }}>{'Chart line belum tersedia (v1)'}</UiTypography>
      </UiCard>
      <UiCard sx={{ p: 2 }}>
        <UiTypography variant="h6">{'Item Terbaru'}</UiTypography>
        <UiTable size="small">
          <UiTableHead>
            <UiTableRow>
              <UiTableCell>{'Name'}</UiTableCell>
              <UiTableCell>{'Status'}</UiTableCell>
            </UiTableRow>
          </UiTableHead>
          <UiTableBody>
          {(((data['listItems'] as Record<string, unknown> | undefined)?.['rows'] as unknown[] | undefined) ?? []).slice(0, 5).map((row, i) => (
            <UiTableRow key={i}>
              <UiTableCell><>{String((row as Record<string, unknown>)?.['name'] ?? '')}</></UiTableCell>
              <UiTableCell><UiChip size="small" label={String((row as Record<string, unknown>)?.['status'] ?? '')} /></UiTableCell>
            </UiTableRow>
          ))}
          </UiTableBody>
        </UiTable>
      </UiCard>
    </UiBox>
  );
}
