// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Routes lazy + menu sidebar dari pages FEIR (view: list). Di-wire src/bootstrap.tsx.

import { lazy, ReactNode, Suspense } from 'react';
import { Route } from 'react-router-dom';
import type { MenuItemDef } from '../App';
import { UiBox, UiTypography } from './ui';

const ItemsList = lazy(() => import('../pages/items/List'));

export const appRoutes: ReactNode = (
  <>
    <Route path='/items'
      element={
        <Suspense
          fallback={<UiBox sx={{ p: 4 }}><UiTypography>Memuat...</UiTypography></UiBox>}
        ><ItemsList /></Suspense>
      }
    />
  </>
);

export const appMenu: MenuItemDef[] = [
  { key: 'items', path: '/items', icon: 'percent', titleKey: 'page.items' },
];
