// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Halaman detail party-rev-sharing-algorithms (detail mode page). Data dari GET retrieve (api defs).
// pageContext binding {{prefix.field}} di-resolve emit-time -> accessor data detail
// (route param :id) - runtime QueryContext dan src/gen/ tidak disentuh.
// Tab/blocks dari FEIR detail.sections: description-list + table (renderColumns nested).

import { ReactElement, useCallback, useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { ArrowLeft, Pencil, Trash2 } from 'lucide-react';
import { ApiError } from '../../gen/apiClient';
import { deletePartyRevSharingAlgorithm, getPartyRevSharingAlgorithm } from '../../gen/api/party-rev-sharing-algorithm';
import type { PartyRevSharingAlgorithm } from '../../gen/api/party-rev-sharing-algorithm';
import { UiAlert, UiBox, UiButton, UiCard, UiIconButton, UiTable, UiTableBody, UiTableCell, UiTableHead, UiTableRow, UiTabs, UiTab, UiTypography } from '../../gen/ui';
import EditForm from './EditForm';

export default function Detail(): ReactElement {
  const { id } = useParams();
  const navigate = useNavigate();
  const [data, setData] = useState<PartyRevSharingAlgorithm | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const [tab, setTab] = useState(0);
  const [editOpen, setEditOpen] = useState(false);

  const load = useCallback(async () => {
    if (id === undefined) return;
    setLoading(true);
    setError(null);
    try {
      const detail = await getPartyRevSharingAlgorithm(id);
      setData(detail);
    } catch (err) {
      setError(err instanceof ApiError ? err.message : String(err));
    } finally {
      setLoading(false);
    }
  }, [id]);

  useEffect(() => {
    void load();
  }, [load]);

  const backToList = (): void => {
    navigate('/party-rev-sharing-algorithms');
  };

  const handleDelete = (): void => {
    if (id === undefined) return;
    if (window.confirm('Hapus data ini? Klik OK untuk melanjutkan.')) {
      deletePartyRevSharingAlgorithm(id)
        .then(() => {
          backToList();
        })
        .catch((err) => {
          setError(err instanceof ApiError ? err.message : String(err));
        });
    }
  };

  if (loading) {
    return (<UiBox sx={{ p: 4 }}><UiTypography>Memuat...</UiTypography></UiBox>);
  }
  if (error !== null) {
    return (<UiBox sx={{ p: 4 }}><UiAlert severity="error">{error}</UiAlert></UiBox>);
  }
  if (data === null) {
    return (<UiBox sx={{ p: 4 }}><UiTypography>Data tidak ditemukan</UiTypography></UiBox>);
  }

  return (
    <UiBox sx={{ display: 'flex', flexDirection: 'column', gap: 2, minHeight: '60vh' }}>
      <UiBox sx={{ display: 'flex', alignItems: 'flex-start', gap: 1, flexWrap: 'wrap' }}>
        <UiIconButton size="small" title="Kembali" aria-label="Kembali" onClick={backToList}><ArrowLeft size={18} /></UiIconButton>
        <UiBox sx={{ flexGrow: 1, minWidth: 240 }}>
          <UiTypography variant="h5">{String((data as Record<string, unknown>)?.["name"] ?? '')}</UiTypography>
          <UiTypography variant="body2" sx={{ color: 'text.secondary' }}>{String((data as Record<string, unknown>)?.["id"] ?? '')}</UiTypography>
        </UiBox>
        <UiBox sx={{ display: 'flex', gap: 1 }}>
          <UiButton variant='primary' startIcon={<Pencil size={16} />} onClick={() => setEditOpen(true)}>{'Edit'}</UiButton>
          <UiButton variant='primary' startIcon={<Trash2 size={16} />} onClick={handleDelete}>{'Hapus'}</UiButton>
        </UiBox>
      </UiBox>
      <UiBox sx={{ width: '100%' }}>
        <UiTabs value={tab} onChange={(_e: unknown, v: number) => setTab(v)}>
          <UiTab label={'Ringkasan'} />
          <UiTab label={'Policy'} />
          <UiTab label={'Condition Variables'} />
          <UiTab label={'Action Variables'} />
        </UiTabs>
      </UiBox>
      <UiBox sx={{ width: '100%' }}>
        {(() => {
          switch (tab) {
            case 0: return <Tab0Body data={data} />;
            case 1: return <Tab1Body data={data} />;
            case 2: return <Tab2Body data={data} />;
            case 3: return <Tab3Body data={data} />;
            default: return <Tab0Body data={data} />;
          }
        })()}
      </UiBox>
      {editOpen && id !== undefined ? <EditForm open={editOpen} id={id} onClose={() => setEditOpen(false)} onSaved={() => void load()} /> : null}
    </UiBox>
  );
}

function Tab0Body(props: { data: PartyRevSharingAlgorithm }): ReactElement {
  const data = props.data;
  return (
    <UiBox sx={{ p: 1 }}>
        <UiCard sx={{ p: 2 }}>
          <UiBox sx={{ display: 'flex', flexDirection: 'column', gap: 0.5 }}>
            <UiBox sx={{ display: 'flex', gap: 1, alignItems: 'flex-start' }}>
              <UiTypography variant="body2" sx={{ width: 170, flexShrink: 0, color: 'text.secondary' }}>{'Nama'}</UiTypography>
              <UiTypography variant="body2" sx={{ wordBreak: 'break-word' }}>{String((data as Record<string, unknown>)?.["name"] ?? '')}</UiTypography>
            </UiBox>
            <UiBox sx={{ display: 'flex', gap: 1, alignItems: 'flex-start' }}>
              <UiTypography variant="body2" sx={{ width: 170, flexShrink: 0, color: 'text.secondary' }}>{'Deskripsi'}</UiTypography>
              <UiTypography variant="body2" sx={{ wordBreak: 'break-word' }}>{String((data as Record<string, unknown>)?.["description"] ?? '')}</UiTypography>
            </UiBox>
            <UiBox sx={{ display: 'flex', gap: 1, alignItems: 'flex-start' }}>
              <UiTypography variant="body2" sx={{ width: 170, flexShrink: 0, color: 'text.secondary' }}>{'@type'}</UiTypography>
              <UiTypography variant="body2" sx={{ wordBreak: 'break-word' }}>{String((data as Record<string, unknown>)?.["@type"] ?? '')}</UiTypography>
            </UiBox>
            <UiBox sx={{ display: 'flex', gap: 1, alignItems: 'flex-start' }}>
              <UiTypography variant="body2" sx={{ width: 170, flexShrink: 0, color: 'text.secondary' }}>{'ID'}</UiTypography>
              <UiTypography variant="body2" sx={{ wordBreak: 'break-word', fontFamily: 'ui-monospace, SFMono-Regular, Menlo, monospace', fontSize: 13 }}>{String((data as Record<string, unknown>)?.["id"] ?? '')}</UiTypography>
            </UiBox>
            <UiBox sx={{ display: 'flex', gap: 1, alignItems: 'flex-start' }}>
              <UiTypography variant="body2" sx={{ width: 170, flexShrink: 0, color: 'text.secondary' }}>{'Href'}</UiTypography>
              <UiTypography variant="body2"><a href={String((data as Record<string, unknown>)?.["href"] ?? '')} target="_blank" rel="noreferrer" style={{ color: 'inherit' }}>{String((data as Record<string, unknown>)?.["href"] ?? '')}</a></UiTypography>
            </UiBox>
          </UiBox>
        </UiCard>
    </UiBox>
  );
}

function Tab1Body(props: { data: PartyRevSharingAlgorithm }): ReactElement {
  const data = props.data;
  return (
    <UiBox sx={{ p: 1 }}>
          {(() => {
            const arr = (data as Record<string, unknown>)?.["policy"];
            if (!arr || !Array.isArray(arr) || arr.length === 0) {
              return <UiTypography variant="body2">{'Belum ada Policy'}</UiTypography>;
            }
            return (
              <UiTable size="small" sx={{ minWidth: 480 }}>
                <UiTableHead>
                  <UiTableRow>
                    <UiTableCell>{'@referredType'}</UiTableCell>
                    <UiTableCell>{'href'}</UiTableCell>
                    <UiTableCell>{'id'}</UiTableCell>
                    <UiTableCell>{'name'}</UiTableCell>
                    <UiTableCell>{'version'}</UiTableCell>
                  </UiTableRow>
                </UiTableHead>
                <UiTableBody>
                  {(arr as unknown[]).map((row, i) => (
                    <UiTableRow key={i}>
                      <UiTableCell>{String((row as Record<string, unknown>)?.["@referredType"] ?? '')}</UiTableCell>
                      <UiTableCell>{String((row as Record<string, unknown>)?.["href"] ?? '')}</UiTableCell>
                      <UiTableCell>{String((row as Record<string, unknown>)?.["id"] ?? '')}</UiTableCell>
                      <UiTableCell>{String((row as Record<string, unknown>)?.["name"] ?? '')}</UiTableCell>
                      <UiTableCell>{String((row as Record<string, unknown>)?.["version"] ?? '')}</UiTableCell>
                    </UiTableRow>
                  ))}
                </UiTableBody>
              </UiTable>
            );
          })()}
    </UiBox>
  );
}

function Tab2Body(props: { data: PartyRevSharingAlgorithm }): ReactElement {
  const data = props.data;
  return (
    <UiBox sx={{ p: 1 }}>
          {(() => {
            const arr = (data as Record<string, unknown>)?.["conditionVariable"];
            if (!arr || !Array.isArray(arr) || arr.length === 0) {
              return <UiTypography variant="body2">{'Belum ada Condition Variable'}</UiTypography>;
            }
            return (
              <UiTable size="small" sx={{ minWidth: 480 }}>
                <UiTableHead>
                  <UiTableRow>
                    <UiTableCell>{'value'}</UiTableCell>
                    <UiTableCell>{'Policy Condition'}</UiTableCell>
                    <UiTableCell>{'Policy Condition Variable'}</UiTableCell>
                  </UiTableRow>
                </UiTableHead>
                <UiTableBody>
                  {(arr as unknown[]).map((row, i) => (
                    <UiTableRow key={i}>
                      <UiTableCell>{String((row as Record<string, unknown>)?.["value"] ?? '')}</UiTableCell>
                      <UiTableCell>{String((((row as Record<string, unknown>)?.["policyCondition"] as Record<string, unknown> | undefined))?.["name"] ?? '')}</UiTableCell>
                      <UiTableCell>{String((((row as Record<string, unknown>)?.["policyConditionVariable"] as Record<string, unknown> | undefined))?.["name"] ?? '')}</UiTableCell>
                    </UiTableRow>
                  ))}
                </UiTableBody>
              </UiTable>
            );
          })()}
    </UiBox>
  );
}

function Tab3Body(props: { data: PartyRevSharingAlgorithm }): ReactElement {
  const data = props.data;
  return (
    <UiBox sx={{ p: 1 }}>
          {(() => {
            const arr = (data as Record<string, unknown>)?.["actionVariable"];
            if (!arr || !Array.isArray(arr) || arr.length === 0) {
              return <UiTypography variant="body2">{'Belum ada Action Variable'}</UiTypography>;
            }
            return (
              <UiTable size="small" sx={{ minWidth: 480 }}>
                <UiTableHead>
                  <UiTableRow>
                    <UiTableCell>{'value'}</UiTableCell>
                    <UiTableCell>{'Policy Action'}</UiTableCell>
                    <UiTableCell>{'Policy Action Variable'}</UiTableCell>
                  </UiTableRow>
                </UiTableHead>
                <UiTableBody>
                  {(arr as unknown[]).map((row, i) => (
                    <UiTableRow key={i}>
                      <UiTableCell>{String((row as Record<string, unknown>)?.["value"] ?? '')}</UiTableCell>
                      <UiTableCell>{String((((row as Record<string, unknown>)?.["policyAction"] as Record<string, unknown> | undefined))?.["name"] ?? '')}</UiTableCell>
                      <UiTableCell>{String((((row as Record<string, unknown>)?.["policyActionVariable"] as Record<string, unknown> | undefined))?.["name"] ?? '')}</UiTableCell>
                    </UiTableRow>
                  ))}
                </UiTableBody>
              </UiTable>
            );
          })()}
    </UiBox>
  );
}
