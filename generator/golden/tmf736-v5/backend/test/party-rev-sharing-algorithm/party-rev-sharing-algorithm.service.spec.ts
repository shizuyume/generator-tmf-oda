// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
/**
 * PartyRevSharingAlgorithm
 *
 * The generated service and controller, against mocked repositories.
 *
 * TypeORM is mocked rather than run: a service emitted with --db postgres
 * carries postgres column types and will not boot on sqlite, so an in-memory
 * database is not an option.
 *
 * Every literal below is derived from this resource, never hand-written: see
 * the mapping table in emit/spec/resource.mjs for where each one comes from.
 */
import { PartyRevSharingAlgorithmService } from '../../src/party-rev-sharing-algorithm/party-rev-sharing-algorithm.service';
import { PartyRevSharingAlgorithmController } from '../../src/party-rev-sharing-algorithm/party-rev-sharing-algorithm.controller';
import { RevenueSharingAlgorithmEventType } from '../../src/event/event-types';
import { PartyRevSharingAlgorithm } from '../../src/party-rev-sharing-algorithm/entities/party-rev-sharing-algorithm.entity';
import { PartyRevSharingPolicyActionVariable } from '../../src/party-rev-sharing-algorithm/entities/party-rev-sharing-policy-action-variable.entity';
import { PartyRevSharingPolicyConditionVariable } from '../../src/party-rev-sharing-algorithm/entities/party-rev-sharing-policy-condition-variable.entity';
import { PolicyActionRef } from '../../src/party-rev-sharing-algorithm/entities/policy-action-ref.entity';
import { PolicyConditionRef } from '../../src/party-rev-sharing-algorithm/entities/policy-condition-ref.entity';
import { PolicyRef } from '../../src/party-rev-sharing-algorithm/entities/policy-ref.entity';
import { PolicyVariableRef } from '../../src/party-rev-sharing-algorithm/entities/policy-variable-ref.entity';

type AnyRec = Record<string, any>;

function makeRepo(): AnyRec {
  return {
    create: jest.fn(() => ({}) as AnyRec),
    save: jest.fn(async (e: AnyRec) => e),
    findOne: jest.fn(),
    createQueryBuilder: jest.fn(),
  };
}

function makeQueryBuilder(rows: AnyRec[]): AnyRec {
  const qb: AnyRec = {
    leftJoinAndSelect: jest.fn(() => qb),
    where: jest.fn(() => qb),
    andWhere: jest.fn(() => qb),
    addOrderBy: jest.fn(() => qb),
    skip: jest.fn(() => qb),
    take: jest.fn(() => qb),
    getCount: jest.fn(async () => rows.length),
    getMany: jest.fn(async () => rows),
  };
  return qb;
}

const HREF =
  '/tmf-api/revenueSharingAlgorithmManagement/v5/partyRevSharingAlgorithm/res-1';

/** The owner-bound collections this resource maps. */
const COLLECTIONS = ['policy', 'conditionVariable', 'actionVariable'] as const;

/** The path segment each collection's row mapper writes into its href. */
const HREF_SEGMENT: Record<string, string> = {
  conditionVariable: 'conditionVariable',
  actionVariable: 'actionVariable',
};

/** The `@type` each collection's rows carry: their own entity class name. */
const ROW_AT_TYPE: Record<string, string> = {
  policy: 'PolicyRef',
  conditionVariable: 'PartyRevSharingPolicyConditionVariable',
  actionVariable: 'PartyRevSharingPolicyActionVariable',
};

function row(collection: string, index: number): AnyRec {
  return {
    id: `${collection}-row-${index}`,
    refId: `${collection}-ref-${index}`,
    atType: ROW_AT_TYPE[collection],
    sortOrder: index,
  };
}

function entity(over: AnyRec = {}): AnyRec {
  return {
    id: 'res-1',
    name: 'res-name',
    description: 'res-description',
    atType: 'PartyRevSharingAlgorithm',
    atSchemaLocation: 'res-atSchemaLocation',
    atBaseType: 'res-atBaseType',
    policy: [row('policy', 0)],
    conditionVariable: [row('conditionVariable', 0)],
    actionVariable: [row('actionVariable', 0)],
    ...over,
  };
}

describe('PartyRevSharingAlgorithm entities', () => {
  it('are constructible, so the mocked repositories stand in for real rows', () => {
    for (const E of [
      PartyRevSharingAlgorithm,
      PartyRevSharingPolicyActionVariable,
      PartyRevSharingPolicyConditionVariable,
      PolicyActionRef,
      PolicyConditionRef,
      PolicyRef,
      PolicyVariableRef,
    ]) {
      expect(new (E as new () => object)()).toBeInstanceOf(E as never);
    }
  });
});
