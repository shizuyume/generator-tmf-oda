// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, ManyToOne, JoinColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { PartyRevSharingAlgorithm } from './party-rev-sharing-algorithm.entity';

/**
 * The policy type algorithm to calculate revenue share
 */
@Entity('policy_ref')
export class PolicyRef {
  @PrimaryColumn({ type: 'varchar', length: 100 })
  id: string;

  @Column({ type: 'int', nullable: true })
  sortOrder?: number;

  /** The actual type of the target instance when needed for disambiguation. */
  @Column({ type: 'varchar', length: 100, nullable: true })
  referredType?: string;

  /** Hyperlink reference */
  @Column({ type: 'varchar', length: 1000, nullable: true })
  href?: string;

  /** unique identifier */
  @Column({ type: 'varchar', length: 36, nullable: true })
  refId?: string;

  /** Name of the referred entity. */
  @Column({ type: 'varchar', length: 255, nullable: true })
  name?: string;

  @Column({ type: 'varchar', length: 50, nullable: true })
  version?: string;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atType?: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  @ManyToOne(() => PartyRevSharingAlgorithm, (parent) => parent.policy, {
    onDelete: 'CASCADE',
    nullable: false,
  })
  @JoinColumn({ name: 'party_rev_sharing_algorithm_id' })
  owner: PartyRevSharingAlgorithm;
}
