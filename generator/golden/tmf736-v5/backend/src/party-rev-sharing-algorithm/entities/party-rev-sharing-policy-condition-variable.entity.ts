// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, ManyToOne, JoinColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { PartyRevSharingAlgorithm } from './party-rev-sharing-algorithm.entity';
import { PolicyConditionRef } from './policy-condition-ref.entity';
import { PolicyVariableRef } from './policy-variable-ref.entity';

/**
 * The policy condition variables of the policy to calculate revenue share
 */
@Entity('party_rev_sharing_policy_condition_variable')
export class PartyRevSharingPolicyConditionVariable {
  @PrimaryColumn({ type: 'varchar', length: 100 })
  id: string;

  /** The id the client gave this item. Unique within its parent only, so it is not the primary key; served back as `id`. */
  @Column({ type: 'varchar', length: 100, nullable: true })
  refId?: string;

  @Column({ type: 'int', nullable: true })
  sortOrder?: number;

  /** The value of variable */
  @Column({ type: 'varchar', length: 255, nullable: true })
  value?: string;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atType?: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  @ManyToOne(() => PartyRevSharingAlgorithm, (parent) => parent.conditionVariable, {
    onDelete: 'CASCADE',
    nullable: false,
  })
  @JoinColumn({ name: 'party_rev_sharing_algorithm_id' })
  owner: PartyRevSharingAlgorithm;

  // normalised single ref: own table, FK held here
  @ManyToOne(() => PolicyConditionRef, {
    cascade: true,
    eager: true,
    nullable: true,
  })
  @JoinColumn({ name: 'policy_condition_id' })
  policyCondition?: PolicyConditionRef;

  // normalised single ref: own table, FK held here
  @ManyToOne(() => PolicyVariableRef, {
    cascade: true,
    eager: true,
    nullable: true,
  })
  @JoinColumn({ name: 'policy_condition_variable_id' })
  policyConditionVariable?: PolicyVariableRef;
}
