// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, OneToMany, CreateDateColumn, UpdateDateColumn, DeleteDateColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { PartyRevSharingPolicyActionVariable } from './party-rev-sharing-policy-action-variable.entity';
import { PartyRevSharingPolicyConditionVariable } from './party-rev-sharing-policy-condition-variable.entity';
import { PolicyRef } from './policy-ref.entity';

/**
 * Base entity schema for use in TMForum Open-APIs. Property.
 */
@Entity('party_rev_sharing_algorithm')
export class PartyRevSharingAlgorithm {
  @PrimaryColumn({ type: 'varchar', length: 100 })
  id: string;

  /** The name of party revenue sharing algorithm */
  @Column({ type: 'varchar', length: 255 })
  name: string;

  /** The description of party revenue sharing algorithm */
  @Column({ type: 'text', nullable: true })
  description?: string;

  @DeleteDateColumn({ nullable: true })
  deletedAt?: Date;

  @Column({ type: 'varchar', length: 100, nullable: true })
  deletedBy?: string;

  @Column({ type: 'varchar', length: 255, nullable: true })
  deletedReason?: string;

  @CreateDateColumn()
  createdDate: Date;

  @UpdateDateColumn()
  lastUpdate: Date;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, default: 'PartyRevSharingAlgorithm' })
  atType: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  @OneToMany(() => PolicyRef, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  policy: PolicyRef[];

  @OneToMany(() => PartyRevSharingPolicyConditionVariable, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  conditionVariable: PartyRevSharingPolicyConditionVariable[];

  @OneToMany(() => PartyRevSharingPolicyActionVariable, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  actionVariable: PartyRevSharingPolicyActionVariable[];
}
