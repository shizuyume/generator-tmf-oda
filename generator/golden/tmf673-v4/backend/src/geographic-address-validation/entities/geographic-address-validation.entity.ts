// GENERATED by tmfgen - DO NOT EDIT. Re-run the generator instead.
import { Entity, PrimaryColumn, Column, ManyToOne, OneToMany, JoinColumn, CreateDateColumn, UpdateDateColumn, DeleteDateColumn } from 'typeorm';
import { Expose } from 'class-transformer';
import { GeographicAddress } from '../../geographic-address/entities/geographic-address.entity';
import { GeographicAddressValidationAlternateGeographicAddress } from './geographic-address-validation-alternate-geographic-address.entity';

/**
 * This resource is used to manage address validation request and response
 */
@Entity('geographic_address_validation')
export class GeographicAddressValidation {
  @PrimaryColumn({ type: 'varchar', length: 36 })
  id: string;

  /** Indicator provided by the requester to specify if alternate addresses must be provided in case of partial or fail result. */
  @Column({ type: 'boolean' })
  provideAlternative: boolean;

  /** Date when the address validation is performed */
  @Column({ type: 'datetime', nullable: true })
  validationDate?: Date;

  /** Result of the address validation (success, partial, fails) */
  @Column({ type: 'varchar', length: 255, nullable: true })
  validationResult?: string;

  /** Possible values for the state of a task */
  /** Allowed: accepted | terminatedWithError | inProgress | done */
  @Column({ type: 'varchar', length: 100, nullable: true })
  state?: string;

  @DeleteDateColumn({ nullable: true })
  deletedAt?: Date;

  @Column({ type: 'varchar', length: 100, nullable: true })
  deletedBy?: string;

  @Column({ type: 'varchar', length: 255, nullable: true })
  deletedReason?: string;

  @CreateDateColumn()
  createdDate: Date;

  @UpdateDateColumn()
  lastUpdate: Date;

  @Expose({ name: '@type' })
  @Column({ type: 'varchar', length: 100, default: 'GeographicAddressValidation' })
  atType: string;

  @Expose({ name: '@schemaLocation' })
  @Column({ type: 'varchar', length: 1000, nullable: true })
  atSchemaLocation?: string;

  @Expose({ name: '@baseType' })
  @Column({ type: 'varchar', length: 100, nullable: true })
  atBaseType?: string;

  // reference to a sibling resource of this service
  @ManyToOne(() => GeographicAddress, {
    eager: true,
    nullable: true,
  })
  @JoinColumn({ name: 'submitted_geographic_address_id' })
  submittedGeographicAddress?: GeographicAddress;

  // reference to a sibling resource of this service
  @ManyToOne(() => GeographicAddress, {
    eager: true,
    nullable: true,
  })
  @JoinColumn({ name: 'valid_geographic_address_id' })
  validGeographicAddress?: GeographicAddress;

  @OneToMany(() => GeographicAddressValidationAlternateGeographicAddress, (child) => child.owner, {
    cascade: true,
    eager: true,
    orphanedRowAction: 'delete',
  })
  alternateGeographicAddress: GeographicAddressValidationAlternateGeographicAddress[];
}
