// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Api defs Categories dari FEIR api.endpoints (fe-spec-retail-admin-console).
// Method HANYA untuk endpoint yang ADA di FEIR - tidak meng-invent endpoint.
// Path = {{api.basePath}} di-resolve emit-time ke ir.api.basePath (bindingSources.api, M2).

import { apiRequest } from '../apiClient';
import type { ApiEnvelope } from '../apiClient';

const RES_BASE = '/api/retail/categories';

export interface Categories {
  id?: string;
  href?: string;
  name?: string;
  description?: string;
  [key: string]: unknown;
}

export interface ListCategoriesParams {
  offset?: number;
  limit?: number;
}

export interface ListCategoriesResult {
  rows: Categories[];
  total: number;
}

export async function listCategories(params: ListCategoriesParams = {}): Promise<ListCategoriesResult> {
  const envelope: ApiEnvelope<Categories[]> = await apiRequest(RES_BASE, { method: 'GET', params: { ...params } });
  const rows = envelope.data ?? [];
  return { rows, total: envelope.total ?? rows.length };
}

export async function getCategories(id: string | number): Promise<Categories | null> {
  const envelope: ApiEnvelope<Categories> = await apiRequest(RES_BASE + '/' + String(id), { method: 'GET' });
  return envelope.data;
}

export async function createCategories(payload: Record<string, unknown>): Promise<Categories | null> {
  const envelope: ApiEnvelope<Categories> = await apiRequest(RES_BASE, { method: 'POST', body: payload });
  return envelope.data;
}

export async function updateCategories(id: string | number, payload: Record<string, unknown>): Promise<Categories | null> {
  const envelope: ApiEnvelope<Categories> = await apiRequest(RES_BASE + '/' + String(id), { method: 'PATCH', body: payload });
  return envelope.data;
}

export async function deleteCategories(id: string | number): Promise<void> {
  await apiRequest<never>(RES_BASE + '/' + String(id), { method: 'DELETE' });
}
