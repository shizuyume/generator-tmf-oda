// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Api defs Orders dari FEIR api.endpoints (fe-spec-retail-admin-console).
// Method HANYA untuk endpoint yang ADA di FEIR - tidak meng-invent endpoint.
// Path = {{api.basePath}} di-resolve emit-time ke ir.api.basePath (bindingSources.api, M2).

import { apiRequest } from '../apiClient';
import type { ApiEnvelope } from '../apiClient';

const RES_BASE = '/api/retail/orders';

export interface Orders {
  id?: string;
  href?: string;
  name?: string;
  description?: string;
  [key: string]: unknown;
}

export interface ListOrdersParams {
  offset?: number;
  limit?: number;
  q?: string;
}

export interface ListOrdersResult {
  rows: Orders[];
  total: number;
}

export async function listOrders(params: ListOrdersParams = {}): Promise<ListOrdersResult> {
  const envelope: ApiEnvelope<Orders[]> = await apiRequest(RES_BASE, { method: 'GET', params: { ...params } });
  const rows = envelope.data ?? [];
  return { rows, total: envelope.total ?? rows.length };
}

export async function getOrders(id: string | number): Promise<Orders | null> {
  const envelope: ApiEnvelope<Orders> = await apiRequest(RES_BASE + '/' + String(id), { method: 'GET' });
  return envelope.data;
}

export async function createOrders(payload: Record<string, unknown>): Promise<Orders | null> {
  const envelope: ApiEnvelope<Orders> = await apiRequest(RES_BASE, { method: 'POST', body: payload });
  return envelope.data;
}

export async function updateOrders(id: string | number, payload: Record<string, unknown>): Promise<Orders | null> {
  const envelope: ApiEnvelope<Orders> = await apiRequest(RES_BASE + '/' + String(id), { method: 'PATCH', body: payload });
  return envelope.data;
}

export async function deleteOrders(id: string | number): Promise<void> {
  await apiRequest<never>(RES_BASE + '/' + String(id), { method: 'DELETE' });
}
