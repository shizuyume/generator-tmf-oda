// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Zod schemas + FieldSpec (kontrak StandardFormModal) + payload spec (api.payload)
// untuk form add/edit halaman ini. FieldSpec adalah SATU-SATUNYA sumber yang diserahkan
// ke StandardFormModal runtime (modal membangun schema zod internal dari zodRules).
// zodSchema di bawah = mirror eksplisit (di-assert fe-payload-check; dibutuhkan tsc).
import { z } from 'zod';
import type { FieldSpec } from '../../gen/StandardFormModal';

export const addFields: FieldSpec[] = [
    {
      name: "name",
      type: "text",
      label: "Nama Lengkap",
      required: true,
      zodRules: [{ rule: "required", message: "Wajib diisi" }, { rule: "minLength", value: 3, message: "Minimal 3 karakter" }]
    },
    {
      name: "email",
      type: "email",
      label: "Email",
      required: true,
      zodRules: [{ rule: "required", message: "Wajib diisi" }, { rule: "email", message: "Format email tidak valid" }]
    },
    {
      name: "department",
      type: "select",
      label: "Departemen",
      required: true,
      options: {"sales":"Penjualan","engineering":"Teknik","finance":"Keuangan","operations":"Operasional","hr":"SDM"},
      zodRules: [{ rule: "required", message: "Wajib diisi" }]
    },
    {
      name: "role",
      type: "radio",
      label: "Jabatan",
      required: true,
      options: {"manager":"Manajer","staff":"Staf","intern":"Magang"},
      layout: "inline",
      zodRules: [{ rule: "required", message: "Wajib diisi" }]
    },
    {
      name: "hireDate",
      type: "date",
      label: "Tanggal Masuk"
    },
    {
      name: "isActive",
      type: "checkbox",
      label: "Status Aktif"
    }
  ];

export const editFields: FieldSpec[] = [
    {
      name: "name",
      type: "text",
      label: "Nama Lengkap",
      required: true,
      zodRules: [{ rule: "required", message: "Wajib diisi" }, { rule: "minLength", value: 3, message: "Minimal 3 karakter" }]
    },
    {
      name: "email",
      type: "email",
      label: "Email",
      required: true,
      zodRules: [{ rule: "required", message: "Wajib diisi" }, { rule: "email", message: "Format email tidak valid" }]
    },
    {
      name: "department",
      type: "select",
      label: "Departemen",
      required: true,
      options: {"sales":"Penjualan","engineering":"Teknik","finance":"Keuangan","operations":"Operasional","hr":"SDM"},
      zodRules: [{ rule: "required", message: "Wajib diisi" }]
    },
    {
      name: "role",
      type: "radio",
      label: "Jabatan",
      required: true,
      options: {"manager":"Manajer","staff":"Staf","intern":"Magang"},
      layout: "inline",
      zodRules: [{ rule: "required", message: "Wajib diisi" }]
    },
    {
      name: "hireDate",
      type: "date",
      label: "Tanggal Masuk"
    },
    {
      name: "isActive",
      type: "checkbox",
      label: "Status Aktif"
    }
  ];

export const addDefaults: Record<string, unknown> = {"name":"","email":"","department":"","role":"","hireDate":"","isActive":false};
export const editDefaultsBase: Record<string, unknown> = {"name":"","email":"","department":"","role":"","hireDate":"","isActive":false};

export const addPayloadSpec: Record<string, unknown> = {"name":{"$src":"fields.name","$tx":"trim"},"email":"fields.email","department":"fields.department","role":"fields.role","hireDate":"fields.hireDate","isActive":"fields.isActive"};
export const editPayloadSpec: Record<string, unknown> = {"name":"fields.name","department":"fields.department","role":"fields.role","isActive":"fields.isActive"};

export const addZodSchema = z.object({ 'name': z.string().min(1, 'Wajib diisi').min(3, 'Minimal 3 karakter'), 'email': z.string().min(1, 'Wajib diisi').email(), 'department': z.string().min(1, 'Wajib diisi'), 'role': z.string().min(1, 'Wajib diisi'), 'hireDate': z.string().optional(), 'isActive': z.boolean().optional() });
export const editZodSchema = z.object({ 'name': z.string().min(1, 'Wajib diisi').min(3, 'Minimal 3 karakter'), 'email': z.string().min(1, 'Wajib diisi').email(), 'department': z.string().min(1, 'Wajib diisi'), 'role': z.string().min(1, 'Wajib diisi'), 'hireDate': z.string().optional(), 'isActive': z.boolean().optional() });

export function mapDetailToDefaults(detail: Record<string, unknown>): Record<string, unknown> {
  const out: Record<string, unknown> = {
    "email": detail["email"] ?? '',
    "department": detail["department"] ?? '',
    "role": detail["role"] ?? '',
    "hireDate": detail["hireDate"] ?? '',
    "isActive": detail["isActive"] ?? ''
  };

  return out;
}
