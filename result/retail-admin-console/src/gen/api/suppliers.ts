// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Api defs Suppliers dari FEIR api.endpoints (fe-spec-retail-admin-console).
// Method HANYA untuk endpoint yang ADA di FEIR - tidak meng-invent endpoint.
// Path = {{api.basePath}} di-resolve emit-time ke ir.api.basePath (bindingSources.api, M2).

import { apiRequest } from '../apiClient';
import type { ApiEnvelope } from '../apiClient';

const RES_BASE = '/api/retail/suppliers';

export interface Suppliers {
  id?: string;
  href?: string;
  name?: string;
  description?: string;
  [key: string]: unknown;
}

export interface ListSuppliersParams {
  offset?: number;
  limit?: number;
  q?: string;
}

export interface ListSuppliersResult {
  rows: Suppliers[];
  total: number;
}

export async function listSuppliers(params: ListSuppliersParams = {}): Promise<ListSuppliersResult> {
  const envelope: ApiEnvelope<Suppliers[]> = await apiRequest(RES_BASE, { method: 'GET', params: { ...params } });
  const rows = envelope.data ?? [];
  return { rows, total: envelope.total ?? rows.length };
}

export async function getSuppliers(id: string | number): Promise<Suppliers | null> {
  const envelope: ApiEnvelope<Suppliers> = await apiRequest(RES_BASE + '/' + String(id), { method: 'GET' });
  return envelope.data;
}

export async function createSuppliers(payload: Record<string, unknown>): Promise<Suppliers | null> {
  const envelope: ApiEnvelope<Suppliers> = await apiRequest(RES_BASE, { method: 'POST', body: payload });
  return envelope.data;
}

export async function updateSuppliers(id: string | number, payload: Record<string, unknown>): Promise<Suppliers | null> {
  const envelope: ApiEnvelope<Suppliers> = await apiRequest(RES_BASE + '/' + String(id), { method: 'PATCH', body: payload });
  return envelope.data;
}

export async function deleteSuppliers(id: string | number): Promise<void> {
  await apiRequest<never>(RES_BASE + '/' + String(id), { method: 'DELETE' });
}
