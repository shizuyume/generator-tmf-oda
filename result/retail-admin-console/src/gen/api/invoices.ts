// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Api defs Invoices dari FEIR api.endpoints (fe-spec-retail-admin-console).
// Method HANYA untuk endpoint yang ADA di FEIR - tidak meng-invent endpoint.
// Path = {{api.basePath}} di-resolve emit-time ke ir.api.basePath (bindingSources.api, M2).

import { apiRequest } from '../apiClient';
import type { ApiEnvelope } from '../apiClient';

const RES_BASE = '/api/retail/invoices';

export interface Invoices {
  id?: string;
  href?: string;
  name?: string;
  description?: string;
  [key: string]: unknown;
}

export interface ListInvoicesParams {
  offset?: number;
  limit?: number;
}

export interface ListInvoicesResult {
  rows: Invoices[];
  total: number;
}

export async function listInvoices(params: ListInvoicesParams = {}): Promise<ListInvoicesResult> {
  const envelope: ApiEnvelope<Invoices[]> = await apiRequest(RES_BASE, { method: 'GET', params: { ...params } });
  const rows = envelope.data ?? [];
  return { rows, total: envelope.total ?? rows.length };
}

export async function getInvoices(id: string | number): Promise<Invoices | null> {
  const envelope: ApiEnvelope<Invoices> = await apiRequest(RES_BASE + '/' + String(id), { method: 'GET' });
  return envelope.data;
}

export async function createInvoices(payload: Record<string, unknown>): Promise<Invoices | null> {
  const envelope: ApiEnvelope<Invoices> = await apiRequest(RES_BASE, { method: 'POST', body: payload });
  return envelope.data;
}

export async function updateInvoices(id: string | number, payload: Record<string, unknown>): Promise<Invoices | null> {
  const envelope: ApiEnvelope<Invoices> = await apiRequest(RES_BASE + '/' + String(id), { method: 'PATCH', body: payload });
  return envelope.data;
}

export async function deleteInvoices(id: string | number): Promise<void> {
  await apiRequest<never>(RES_BASE + '/' + String(id), { method: 'DELETE' });
}
