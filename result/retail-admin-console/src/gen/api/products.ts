// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Api defs Products dari FEIR api.endpoints (fe-spec-retail-admin-console).
// Method HANYA untuk endpoint yang ADA di FEIR - tidak meng-invent endpoint.
// Path = {{api.basePath}} di-resolve emit-time ke ir.api.basePath (bindingSources.api, M2).

import { apiRequest } from '../apiClient';
import type { ApiEnvelope } from '../apiClient';

const RES_BASE = '/api/retail/products';

export interface Products {
  id?: string;
  href?: string;
  name?: string;
  description?: string;
  [key: string]: unknown;
}

export interface ListProductsParams {
  offset?: number;
  limit?: number;
  q?: string;
}

export interface ListProductsResult {
  rows: Products[];
  total: number;
}

export async function listProducts(params: ListProductsParams = {}): Promise<ListProductsResult> {
  const envelope: ApiEnvelope<Products[]> = await apiRequest(RES_BASE, { method: 'GET', params: { ...params } });
  const rows = envelope.data ?? [];
  return { rows, total: envelope.total ?? rows.length };
}

export async function getProducts(id: string | number): Promise<Products | null> {
  const envelope: ApiEnvelope<Products> = await apiRequest(RES_BASE + '/' + String(id), { method: 'GET' });
  return envelope.data;
}

export async function createProducts(payload: Record<string, unknown>): Promise<Products | null> {
  const envelope: ApiEnvelope<Products> = await apiRequest(RES_BASE, { method: 'POST', body: payload });
  return envelope.data;
}

export async function updateProducts(id: string | number, payload: Record<string, unknown>): Promise<Products | null> {
  const envelope: ApiEnvelope<Products> = await apiRequest(RES_BASE + '/' + String(id), { method: 'PATCH', body: payload });
  return envelope.data;
}

export async function deleteProducts(id: string | number): Promise<void> {
  await apiRequest<never>(RES_BASE + '/' + String(id), { method: 'DELETE' });
}
