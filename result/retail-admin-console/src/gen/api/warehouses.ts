// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Api defs Warehouses dari FEIR api.endpoints (fe-spec-retail-admin-console).
// Method HANYA untuk endpoint yang ADA di FEIR - tidak meng-invent endpoint.
// Path = {{api.basePath}} di-resolve emit-time ke ir.api.basePath (bindingSources.api, M2).

import { apiRequest } from '../apiClient';
import type { ApiEnvelope } from '../apiClient';

const RES_BASE = '/api/retail/warehouses';

export interface Warehouses {
  id?: string;
  href?: string;
  name?: string;
  description?: string;
  [key: string]: unknown;
}

export interface ListWarehousesParams {
  offset?: number;
  limit?: number;
}

export interface ListWarehousesResult {
  rows: Warehouses[];
  total: number;
}

export async function listWarehouses(params: ListWarehousesParams = {}): Promise<ListWarehousesResult> {
  const envelope: ApiEnvelope<Warehouses[]> = await apiRequest(RES_BASE, { method: 'GET', params: { ...params } });
  const rows = envelope.data ?? [];
  return { rows, total: envelope.total ?? rows.length };
}

export async function getWarehouses(id: string | number): Promise<Warehouses | null> {
  const envelope: ApiEnvelope<Warehouses> = await apiRequest(RES_BASE + '/' + String(id), { method: 'GET' });
  return envelope.data;
}

export async function createWarehouses(payload: Record<string, unknown>): Promise<Warehouses | null> {
  const envelope: ApiEnvelope<Warehouses> = await apiRequest(RES_BASE, { method: 'POST', body: payload });
  return envelope.data;
}

export async function updateWarehouses(id: string | number, payload: Record<string, unknown>): Promise<Warehouses | null> {
  const envelope: ApiEnvelope<Warehouses> = await apiRequest(RES_BASE + '/' + String(id), { method: 'PATCH', body: payload });
  return envelope.data;
}

export async function deleteWarehouses(id: string | number): Promise<void> {
  await apiRequest<never>(RES_BASE + '/' + String(id), { method: 'DELETE' });
}
