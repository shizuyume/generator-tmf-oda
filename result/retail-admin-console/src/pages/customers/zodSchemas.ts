// GENERATED by fe-gen emit - DO NOT EDIT. Re-run `fe-gen emit` instead.
// Zod schemas + FieldSpec (kontrak StandardFormModal) + payload spec (api.payload)
// untuk form add/edit halaman ini. FieldSpec adalah SATU-SATUNYA sumber yang diserahkan
// ke StandardFormModal runtime (modal membangun schema zod internal dari zodRules).
// zodSchema di bawah = mirror eksplisit (di-assert fe-payload-check; dibutuhkan tsc).
import { z } from 'zod';
import type { FieldSpec } from '../../gen/StandardFormModal';

export const addFields: FieldSpec[] = [
    {
      name: "name",
      type: "text",
      label: "Nama Lengkap",
      required: true,
      zodRules: [{ rule: "required", message: "Wajib diisi" }, { rule: "minLength", value: 3, message: "Minimal 3 karakter" }]
    },
    {
      name: "email",
      type: "email",
      label: "Email",
      required: true,
      zodRules: [{ rule: "required", message: "Wajib diisi" }, { rule: "email", message: "Format email tidak valid" }]
    },
    {
      name: "phone",
      type: "text",
      label: "Telepon"
    },
    {
      name: "segment",
      type: "select",
      label: "Segmen",
      required: true,
      options: {"retail":"Retail","corporate":"Korporat","wholesale":"Grosir"},
      zodRules: [{ rule: "required", message: "Wajib diisi" }]
    },
    {
      name: "joinDate",
      type: "date",
      label: "Tanggal Bergabung"
    },
    {
      name: "isVip",
      type: "checkbox",
      label: "Pelanggan VIP"
    }
  ];

export const editFields: FieldSpec[] = [
    {
      name: "name",
      type: "text",
      label: "Nama Lengkap",
      required: true,
      zodRules: [{ rule: "required", message: "Wajib diisi" }, { rule: "minLength", value: 3, message: "Minimal 3 karakter" }]
    },
    {
      name: "email",
      type: "email",
      label: "Email",
      required: true,
      zodRules: [{ rule: "required", message: "Wajib diisi" }, { rule: "email", message: "Format email tidak valid" }]
    },
    {
      name: "phone",
      type: "text",
      label: "Telepon"
    },
    {
      name: "segment",
      type: "select",
      label: "Segmen",
      required: true,
      options: {"retail":"Retail","corporate":"Korporat","wholesale":"Grosir"},
      zodRules: [{ rule: "required", message: "Wajib diisi" }]
    },
    {
      name: "joinDate",
      type: "date",
      label: "Tanggal Bergabung"
    },
    {
      name: "isVip",
      type: "checkbox",
      label: "Pelanggan VIP"
    }
  ];

export const addDefaults: Record<string, unknown> = {"name":"","email":"","phone":"","segment":"","joinDate":"","isVip":false};
export const editDefaultsBase: Record<string, unknown> = {"name":"","email":"","phone":"","segment":"","joinDate":"","isVip":false};

export const addPayloadSpec: Record<string, unknown> = {"name":{"$src":"fields.name","$tx":"trim"},"email":"fields.email","phone":{"$src":"fields.phone","$tx":"trim"},"segment":"fields.segment","joinDate":"fields.joinDate","isVip":"fields.isVip"};
export const editPayloadSpec: Record<string, unknown> = {"name":"fields.name","phone":"fields.phone","segment":"fields.segment","isVip":"fields.isVip"};

export const addZodSchema = z.object({ 'name': z.string().min(1, 'Wajib diisi').min(3, 'Minimal 3 karakter'), 'email': z.string().min(1, 'Wajib diisi').email(), 'phone': z.string().optional(), 'segment': z.string().min(1, 'Wajib diisi'), 'joinDate': z.string().optional(), 'isVip': z.boolean().optional() });
export const editZodSchema = z.object({ 'name': z.string().min(1, 'Wajib diisi').min(3, 'Minimal 3 karakter'), 'email': z.string().min(1, 'Wajib diisi').email(), 'phone': z.string().optional(), 'segment': z.string().min(1, 'Wajib diisi'), 'joinDate': z.string().optional(), 'isVip': z.boolean().optional() });

export function mapDetailToDefaults(detail: Record<string, unknown>): Record<string, unknown> {
  const out: Record<string, unknown> = {
    "email": detail["email"] ?? '',
    "segment": detail["segment"] ?? '',
    "joinDate": detail["joinDate"] ?? '',
    "isVip": detail["isVip"] ?? ''
  };

  return out;
}
