// GENERATED by fe-gen emit mcs-common - DO NOT EDIT. Re-run `fe-gen emit` instead.
// design-lab-only preview harness (see comment in the generated file below) - remove once
// a real host mounts these exposes instead.
import { useState } from 'react';
import './App.css';
import IntentPreview from './exposes/Intent';
import IntentReportPreview from './exposes/IntentReport';
import IntentSpecificationPreview from './exposes/IntentSpecification';
import HubPreview from './exposes/Hub';

type View = 'landing' | "intent" | "intent-report" | "intent-specification" | 'hub';

function App() {
  const [view, setView] = useState<View>('landing');

  if (view === "intent") {
    return (
      <div>
        <button className="preview-back" onClick={() => setView('landing')}>← Back to landing</button>
        <IntentPreview />
      </div>
    );
  }

  if (view === "intent-report") {
    return (
      <div>
        <button className="preview-back" onClick={() => setView('landing')}>← Back to landing</button>
        <IntentReportPreview />
      </div>
    );
  }

  if (view === "intent-specification") {
    return (
      <div>
        <button className="preview-back" onClick={() => setView('landing')}>← Back to landing</button>
        <IntentSpecificationPreview />
      </div>
    );
  }

  if (view === 'hub') {
    return (
      <div>
        <button className="preview-back" onClick={() => setView('landing')}>← Back to landing</button>
        <HubPreview />
      </div>
    );
  }

  return (
    <div className="landing">
      <header className="landing__hero">
        <p className="landing__eyebrow">TM Forum ODA Remote</p>
        <h1>Intent Management</h1>
        <p>Portal manajemen Intent Management (TMF921)</p>
        <div className="landing__actions">
            <button className="preview-trigger" onClick={() => setView("intent")}>
              Preview: Intent
            </button>
            <button className="preview-trigger" onClick={() => setView("intent-report")}>
              Preview: Intent Report
            </button>
            <button className="preview-trigger" onClick={() => setView("intent-specification")}>
              Preview: Intent Specification
            </button>
            <button className="preview-trigger" onClick={() => setView('hub')}>
              Preview: Event Hub
            </button>
        </div>
      </header>
      <section className="landing__section">
        <h2>Federation Contract</h2>
        <p>Remote: <code>intent_management_remote</code></p>
      </section>
      <section className="landing__section">
        <h2>Exposed Resources</h2>
        <div className="pill-list">
            <span className="pill">Intent</span>
            <span className="pill">IntentReport</span>
            <span className="pill">IntentSpecification</span>
        </div>
      </section>
    </div>
  );
}

export default App;
