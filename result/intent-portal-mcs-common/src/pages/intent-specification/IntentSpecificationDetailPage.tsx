// GENERATED by fe-gen emit mcs-common - DO NOT EDIT. Re-run `fe-gen emit` instead.
import { useCallback, useEffect, useState } from 'react';
import CustomBreadcrumbs from 'common_remote/Breadcrumbs';
import CustomButton from 'common_remote/Button';
import CustomDialog from 'common_remote/Dialog';
import CustomSnackbar from 'common_remote/Snackbar';
import CustomTypography from 'common_remote/Typography';
import Box from '@mui/material/Box';
import Stack from '@mui/material/Stack';
import Table from '@mui/material/Table';
import TableBody from '@mui/material/TableBody';
import TableCell from '@mui/material/TableCell';
import TableContainer from '@mui/material/TableContainer';
import TableHead from '@mui/material/TableHead';
import TableRow from '@mui/material/TableRow';
import PillTabs from 'common_remote/PillTabs';
import {
  DetailField,
  SectionCard,
  DETAIL_GRID_SX,
  ViewToggle,
  ViewMode,
} from '../../components/DetailComponents';
import CreateIntentSpecificationDialog from './CreateIntentSpecificationDialog';
import { buildPayload, mapDetailToDefaults } from '../../lib/buildPayload';
import type { ComputedFromRule } from '../../lib/buildPayload';
import { getIntentSpecification, deleteIntentSpecification, patchIntentSpecification, getErrorMessage } from '../../service';
import type { IntentSpecification } from '../../types';

type SnackState = { open: boolean; message: string; severity: 'success' | 'error' };

// TODO: CUSTOMIZE — per-resource template point (endpoint/fields differ per TMF component).
const EDIT_PAYLOAD_SPEC = {
  "name": "fields.name",
  "description": "fields.description",
  "lifecycleStatus": "fields.lifecycleStatus",
  "isBundle": "fields.isBundle",
  "version": "fields.version",
  "attachment": "fields.attachment",
  "specCharacteristic": "fields.specCharacteristic",
  "relatedParty": "fields.relatedParty",
  "constraint": "fields.constraint",
  "entitySpecRelationship": "fields.entitySpecRelationship",
  "intentSpecRelationship": "fields.intentSpecRelationship"
};
const EDIT_REPEATABLE_ITEM_PAYLOADS = {
  "attachment": {
    "@type": "item.atType"
  },
  "specCharacteristic": {
    "@type": "item.atType",
    "name": "item.name",
    "valueType": "item.valueType",
    "description": "item.description",
    "configurable": "item.configurable",
    "minCardinality": "item.minCardinality",
    "maxCardinality": "item.maxCardinality",
    "isUnique": "item.isUnique",
    "regex": "item.regex",
    "extensible": "item.extensible",
    "atValueSchemaLocation": "item.atValueSchemaLocation",
    "validFor": {
      "@type": "TimePeriod"
    }
  },
  "relatedParty": {
    "@type": "item.atType",
    "role": "item.role"
  },
  "constraint": {
    "@type": "item.atType",
    "@referredType": "item.referredType",
    "href": "item.href",
    "id": "item.id",
    "name": "item.name",
    "version": "item.version"
  },
  "entitySpecRelationship": {
    "@type": "item.atType",
    "name": "item.name",
    "role": "item.role",
    "relationshipType": "item.relationshipType",
    "validFor": {
      "@type": "TimePeriod"
    },
    "associationSpec": {
      "@type": "AssociationSpecificationRef",
      "href": "item.associationSpecHref",
      "id": "item.associationSpecId",
      "name": "item.associationSpecName",
      "@referredType": "item.associationSpecReferredType"
    }
  },
  "intentSpecRelationship": {
    "@type": "item.atType",
    "@referredType": "item.referredType",
    "href": "item.href",
    "id": "item.id",
    "name": "item.name",
    "role": "item.role",
    "relationshipType": "item.relationshipType",
    "validFor": {
      "@type": "TimePeriod"
    }
  }
};
const EDIT_COMPUTED_FROM_ROOT: ComputedFromRule[] = [];
const EDIT_COMPUTED_FROM_REPEATABLE: Record<string, ComputedFromRule[]> = {
  "constraint": [
    {
      "target": "href",
      "source": "id"
    }
  ],
  "intentSpecRelationship": [
    {
      "target": "href",
      "source": "id"
    }
  ]
};

interface Props {
  id: string;
  onBack: () => void;
}

export default function IntentSpecificationDetailPage({ id, onBack }: Readonly<Props>) {
  const [data, setData] = useState<IntentSpecification | null>(null);
  const [loading, setLoading] = useState(true);
  const [activeTab, setActiveTab] = useState(0);
  const [snackbar, setSnackbar] = useState<SnackState>({ open: false, message: '', severity: 'success' });
  const [confirmDelete, setConfirmDelete] = useState(false);
  const [editOpen, setEditOpen] = useState(false);
  const [savingEdit, setSavingEdit] = useState(false);
  const [attachmentView, setAttachmentView] = useState<ViewMode>('card');
  const [specCharacteristicView, setSpecCharacteristicView] = useState<ViewMode>('card');
  const [relatedPartyView, setRelatedPartyView] = useState<ViewMode>('card');
  const [constraintView, setConstraintView] = useState<ViewMode>('card');
  const [entitySpecRelationshipView, setEntitySpecRelationshipView] = useState<ViewMode>('card');
  const [intentSpecRelationshipView, setIntentSpecRelationshipView] = useState<ViewMode>('card');

  const loadDetail = useCallback(async () => {
    setLoading(true);
    try {
      const result = await getIntentSpecification(id);
      setData(result);
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    } finally {
      setLoading(false);
    }
  }, [id]);

  useEffect(() => { loadDetail(); }, [loadDetail]);

  const handleDelete = async () => {
    try {
      await deleteIntentSpecification(id);
      setSnackbar({ open: true, message: 'Deleted successfully', severity: 'success' });
      setConfirmDelete(false);
      onBack();
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    }
  };
  const handleEditSave = async (values: Record<string, any>) => {
    setSavingEdit(true);
    try {
      const payload = buildPayload(values, EDIT_PAYLOAD_SPEC, EDIT_REPEATABLE_ITEM_PAYLOADS, EDIT_COMPUTED_FROM_ROOT, EDIT_COMPUTED_FROM_REPEATABLE);
      await patchIntentSpecification(id, payload as any);
      setSnackbar({ open: true, message: 'Saved successfully', severity: 'success' });
      setEditOpen(false);
      await loadDetail();
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    } finally {
      setSavingEdit(false);
    }
  };

  if (loading) {
    return <Box sx={{ p: 3 }}><CustomTypography>Loading...</CustomTypography></Box>;
  }
  if (!data) {
    return (
      <Box sx={{ p: 3 }}>
        <CustomButton label="← Back" variant="text" onClick={onBack} />
        <CustomTypography color="error">Not found</CustomTypography>
      </Box>
    );
  }

  const tab0Content = (
    <>
      <SectionCard>
          <Box sx={DETAIL_GRID_SX}>
            <DetailField label={"Nama"} value={data.name} />
            <DetailField label={"Deskripsi"} value={data.description} />
            <DetailField label={"Last Update"} value={data.lastUpdate} />
            <DetailField label={"Lifecycle Status"} value={data.lifecycleStatus} />
            <DetailField label={"Is Bundle"} value={data.isBundle} />
            <DetailField label={"Version"} value={data.version} />
            <DetailField label={"@type"} value={data["@type"]} />
            <DetailField label={"ID"} value={data.id} />
            <DetailField label={"Href"} value={data.href ? <a href={data.href}>{data.href}</a> : undefined} />
          </Box>
        </SectionCard>
    </>
  );

  const tab1Content = (
    <>
      {(() => { const attachmentItems = data.attachment ?? []; return (
        <SectionCard title={"Attachment"} action={<ViewToggle value={attachmentView} onChange={setAttachmentView} />}>
          {(attachmentItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada Attachment</CustomTypography>
          ) : attachmentView === 'card' ? (
            <Stack spacing={1.5}>
              {(attachmentItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>

                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>

                  </TableRow>
                </TableHead>
                <TableBody>
                  {(attachmentItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>

                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tab2Content = (
    <>
      {(() => { const specCharacteristicItems = data.specCharacteristic ?? []; return (
        <SectionCard title={"Spec Characteristic"} action={<ViewToggle value={specCharacteristicView} onChange={setSpecCharacteristicView} />}>
          {(specCharacteristicItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada Spec Characteristic</CustomTypography>
          ) : specCharacteristicView === 'card' ? (
            <Stack spacing={1.5}>
              {(specCharacteristicItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"name"} value={item["name"]} />
                    <DetailField label={"valueType"} value={item["valueType"]} />
                    <DetailField label={"description"} value={item["description"]} />
                    <DetailField label={"configurable"} value={item["configurable"]} />
                    <DetailField label={"minCardinality"} value={item["minCardinality"]} />
                    <DetailField label={"Valid For"} value={item["validFor"]?.["name"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>name</TableCell>
                    <TableCell>valueType</TableCell>
                    <TableCell>description</TableCell>
                    <TableCell>configurable</TableCell>
                    <TableCell>minCardinality</TableCell>
                    <TableCell>Valid For</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(specCharacteristicItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["name"] ?? '-'}</TableCell>
                      <TableCell>{row["valueType"] ?? '-'}</TableCell>
                      <TableCell>{row["description"] ?? '-'}</TableCell>
                      <TableCell>{row["configurable"] ?? '-'}</TableCell>
                      <TableCell>{row["minCardinality"] ?? '-'}</TableCell>
                      <TableCell>{row["validFor"]?.["name"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tab3Content = (
    <>
      {(() => { const relatedPartyItems = data.relatedParty ?? []; return (
        <SectionCard title={"Related Party"} action={<ViewToggle value={relatedPartyView} onChange={setRelatedPartyView} />}>
          {(relatedPartyItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada Related Party</CustomTypography>
          ) : relatedPartyView === 'card' ? (
            <Stack spacing={1.5}>
              {(relatedPartyItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"role"} value={item["role"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>role</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(relatedPartyItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["role"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tab4Content = (
    <>
      {(() => { const constraintItems = data.constraint ?? []; return (
        <SectionCard title={"Constraint"} action={<ViewToggle value={constraintView} onChange={setConstraintView} />}>
          {(constraintItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada Constraint</CustomTypography>
          ) : constraintView === 'card' ? (
            <Stack spacing={1.5}>
              {(constraintItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"@referredType"} value={item["@referredType"]} />
                    <DetailField label={"href"} value={item["href"]} />
                    <DetailField label={"id"} value={item["id"]} />
                    <DetailField label={"name"} value={item["name"]} />
                    <DetailField label={"version"} value={item["version"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>@referredType</TableCell>
                    <TableCell>href</TableCell>
                    <TableCell>id</TableCell>
                    <TableCell>name</TableCell>
                    <TableCell>version</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(constraintItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["@referredType"] ?? '-'}</TableCell>
                      <TableCell>{row["href"] ?? '-'}</TableCell>
                      <TableCell>{row["id"] ?? '-'}</TableCell>
                      <TableCell>{row["name"] ?? '-'}</TableCell>
                      <TableCell>{row["version"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tab5Content = (
    <>
      {(() => { const entitySpecRelationshipItems = data.entitySpecRelationship ?? []; return (
        <SectionCard title={"Entity Spec Relationship"} action={<ViewToggle value={entitySpecRelationshipView} onChange={setEntitySpecRelationshipView} />}>
          {(entitySpecRelationshipItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada Entity Spec Relationship</CustomTypography>
          ) : entitySpecRelationshipView === 'card' ? (
            <Stack spacing={1.5}>
              {(entitySpecRelationshipItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"name"} value={item["name"]} />
                    <DetailField label={"role"} value={item["role"]} />
                    <DetailField label={"relationshipType"} value={item["relationshipType"]} />
                    <DetailField label={"Valid For"} value={item["validFor"]?.["name"]} />
                    <DetailField label={"Association Spec"} value={item["associationSpec"]?.["name"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>name</TableCell>
                    <TableCell>role</TableCell>
                    <TableCell>relationshipType</TableCell>
                    <TableCell>Valid For</TableCell>
                    <TableCell>Association Spec</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(entitySpecRelationshipItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["name"] ?? '-'}</TableCell>
                      <TableCell>{row["role"] ?? '-'}</TableCell>
                      <TableCell>{row["relationshipType"] ?? '-'}</TableCell>
                      <TableCell>{row["validFor"]?.["name"] ?? '-'}</TableCell>
                      <TableCell>{row["associationSpec"]?.["name"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tab6Content = (
    <>
      {(() => { const intentSpecRelationshipItems = data.intentSpecRelationship ?? []; return (
        <SectionCard title={"Intent Spec Relationship"} action={<ViewToggle value={intentSpecRelationshipView} onChange={setIntentSpecRelationshipView} />}>
          {(intentSpecRelationshipItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada Intent Spec Relationship</CustomTypography>
          ) : intentSpecRelationshipView === 'card' ? (
            <Stack spacing={1.5}>
              {(intentSpecRelationshipItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"@referredType"} value={item["@referredType"]} />
                    <DetailField label={"href"} value={item["href"]} />
                    <DetailField label={"id"} value={item["id"]} />
                    <DetailField label={"name"} value={item["name"]} />
                    <DetailField label={"role"} value={item["role"]} />
                    <DetailField label={"Valid For"} value={item["validFor"]?.["name"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>@referredType</TableCell>
                    <TableCell>href</TableCell>
                    <TableCell>id</TableCell>
                    <TableCell>name</TableCell>
                    <TableCell>role</TableCell>
                    <TableCell>Valid For</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(intentSpecRelationshipItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["@referredType"] ?? '-'}</TableCell>
                      <TableCell>{row["href"] ?? '-'}</TableCell>
                      <TableCell>{row["id"] ?? '-'}</TableCell>
                      <TableCell>{row["name"] ?? '-'}</TableCell>
                      <TableCell>{row["role"] ?? '-'}</TableCell>
                      <TableCell>{row["validFor"]?.["name"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tabs = [
    { label: "Ringkasan", content: tab0Content },
    { label: "Attachment", content: tab1Content },
    { label: "Spec Characteristic", content: tab2Content },
    { label: "Related Party", content: tab3Content },
    { label: "Constraint", content: tab4Content },
    { label: "Entity Spec Relationship", content: tab5Content },
    { label: "Intent Spec Relationship", content: tab6Content }
  ];

  return (
    <Box sx={{ p: 3 }}>
      <CustomBreadcrumbs items={[{ label: "Intent Specification", onClick: onBack }, { label: (data as any).name ?? data.id }]} />

      <Stack direction="row" spacing={1.25} sx={{ mt: 2 }}>
        <CustomButton label="← Back to List" onClick={onBack} variant="outlined" />
        <CustomButton label="Edit" onClick={() => setEditOpen(true)} variant="contained" />
        <CustomButton label="Delete" onClick={() => setConfirmDelete(true)} variant="contained" color="error" />
      </Stack>

      <Box sx={{ mt: 2, mb: 1 }}>
        <CustomTypography variant="h5">{(data as any).name ?? data.id}</CustomTypography>
      </Box>

      <Box sx={{ mt: 2 }}>
        <PillTabs tabs={tabs} value={activeTab} onChange={setActiveTab} />
      </Box>

      <CustomDialog
        open={confirmDelete}
        onClose={() => setConfirmDelete(false)}
        title="Delete"
        content={<CustomTypography>Are you sure you want to delete this record?</CustomTypography>}
        actions={
          <Stack direction="row" spacing={1}>
            <CustomButton label="Cancel" variant="outlined" onClick={() => setConfirmDelete(false)} />
            <CustomButton label="Delete" variant="contained" color="error" onClick={handleDelete} />
          </Stack>
        }
      />

      {editOpen && (
        <CreateIntentSpecificationDialog
          open={editOpen}
          onClose={() => setEditOpen(false)}
          onSave={handleEditSave}
          saving={savingEdit}
          initialValues={mapDetailToDefaults(data as unknown as Record<string, any>, EDIT_PAYLOAD_SPEC, EDIT_REPEATABLE_ITEM_PAYLOADS)}
          title="Edit Intent Specification"
          submitLabel="Save"
        />
      )}

      <CustomSnackbar open={snackbar.open} onClose={() => setSnackbar((s) => ({ ...s, open: false }))} message={snackbar.message} severity={snackbar.severity} />
    </Box>
  );
}
