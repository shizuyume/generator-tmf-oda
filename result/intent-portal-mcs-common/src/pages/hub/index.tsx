// GENERATED by fe-gen emit mcs-common - DO NOT EDIT. Re-run `fe-gen emit` instead.
import { useCallback, useEffect, useState, useRef } from 'react';
import CustomBreadcrumbs from 'common_remote/Breadcrumbs';
import CustomButton from 'common_remote/Button';
import CustomDataGrid from 'common_remote/DataGrid';
import CustomDialog from 'common_remote/Dialog';
import CustomSnackbar from 'common_remote/Snackbar';
import CustomTextField from 'common_remote/TextField';
import CustomTypography from 'common_remote/Typography';
import Box from '@mui/material/Box';
import Stack from '@mui/material/Stack';
import { listHubs, createHub, deleteHub, getErrorMessage } from '../../service';
import type { Hub_FVO } from '../../types/hub';
import type { ListParams } from '../../types';

type SnackState = { open: boolean; message: string; severity: 'success' | 'error' };

const GRID_COLUMNS = [
  { key: 'id', label: 'ID', sortable: true },
  { key: 'callback', label: 'Callback URL', sortable: true },
  { key: 'query', label: 'Query Filter', sortable: false },
];

export default function HubPage() {
  const [rows, setRows] = useState<any[]>([]);
  const [totalRows, setTotalRows] = useState(0);
  const [rowsPerPage, setRowsPerPage] = useState(10);
  const [snackbar, setSnackbar] = useState<SnackState>({ open: false, message: '', severity: 'success' });
  const [queryFilter, setQueryFilter] = useState({ page: 0 });
  const [openCreate, setOpenCreate] = useState(false);
  const [formCallback, setFormCallback] = useState('');
  const [formQuery, setFormQuery] = useState('');
  const [saving, setSaving] = useState(false);
  const [deleteTarget, setDeleteTarget] = useState<string | null>(null);
  const skipNextPageChange = useRef(false);

  const loadData = useCallback(async () => {
    try {
      const params: ListParams = { offset: queryFilter.page * rowsPerPage, limit: rowsPerPage };
      const result = await listHubs(params);
      setRows(result.data.map((item: any) => ({ id: item.id, callback: item.callback, query: item.query || '-' })));
      setTotalRows(result.total);
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    }
  }, [queryFilter, rowsPerPage]);

  useEffect(() => { loadData(); }, [loadData]);

  const handlePageChange = useCallback((newPage: number) => {
    if (skipNextPageChange.current) { skipNextPageChange.current = false; return; }
    setQueryFilter((prev) => ({ ...prev, page: newPage }));
  }, []);

  const handleCreate = async () => {
    if (!formCallback.trim()) return;
    setSaving(true);
    try {
      const body: Hub_FVO = { callback: formCallback.trim() };
      if (formQuery.trim()) body.query = formQuery.trim();
      await createHub(body);
      setSnackbar({ open: true, message: 'Subscription created', severity: 'success' });
      setOpenCreate(false);
      setFormCallback('');
      setFormQuery('');
      await loadData();
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    } finally {
      setSaving(false);
    }
  };

  const handleDelete = async () => {
    if (!deleteTarget) return;
    try {
      await deleteHub(deleteTarget);
      setSnackbar({ open: true, message: 'Subscription removed', severity: 'success' });
      setDeleteTarget(null);
      await loadData();
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    }
  };

  return (
    <Box sx={{ p: 3 }}>
      <CustomBreadcrumbs items={[{ label: "Intent Management" }, { label: 'Event Hub' }]} />

      <Stack direction="row" sx={{ mt: 2, mb: 2, justifyContent: 'space-between', alignItems: 'center' }}>
        <CustomTypography variant="h5">Event Hub Subscriptions</CustomTypography>
        <CustomButton label="New Subscription" variant="contained" onClick={() => setOpenCreate(true)} />
      </Stack>

      <CustomDataGrid
        title="Subscriptions"
        columns={GRID_COLUMNS}
        rows={rows}
        mode="server"
        totalRows={totalRows}
        page={queryFilter.page}
        rowsPerPage={rowsPerPage}
        onPageChange={handlePageChange}
        onRowsPerPageChange={setRowsPerPage}
        onDelete={(row: any) => setDeleteTarget(row.id)}
        actionVariant="inline"
        onRefresh={loadData}
      />

      <CustomDialog
        open={openCreate}
        onClose={() => setOpenCreate(false)}
        title="Create Event Subscription"
        maxWidth="sm"
        fullWidth
        content={
          <Stack spacing={2} sx={{ pt: 1 }}>
            <CustomTextField label="Callback URL" value={formCallback} onChange={setFormCallback} required placeholder="https://your-server.com/webhook" />
            <CustomTextField label="Query Filter (optional)" value={formQuery} onChange={setFormQuery} multiline rows={3} helperText="Filter events by type or criteria" />
          </Stack>
        }
        actions={
          <Stack direction="row" spacing={1}>
            <CustomButton label="Cancel" variant="outlined" onClick={() => setOpenCreate(false)} />
            <CustomButton label="Subscribe" variant="contained" onClick={handleCreate} disabled={saving || !formCallback.trim()} />
          </Stack>
        }
      />

      <CustomDialog
        open={!!deleteTarget}
        onClose={() => setDeleteTarget(null)}
        title="Remove Subscription"
        content={<CustomTypography>Are you sure you want to remove this event subscription?</CustomTypography>}
        actions={
          <Stack direction="row" spacing={1}>
            <CustomButton label="Cancel" variant="outlined" onClick={() => setDeleteTarget(null)} />
            <CustomButton label="Remove" variant="contained" color="error" onClick={handleDelete} />
          </Stack>
        }
      />

      <CustomSnackbar open={snackbar.open} onClose={() => setSnackbar((s) => ({ ...s, open: false }))} message={snackbar.message} severity={snackbar.severity} />
    </Box>
  );
}
