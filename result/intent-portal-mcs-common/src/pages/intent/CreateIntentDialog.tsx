// GENERATED by fe-gen emit mcs-common - DO NOT EDIT. Re-run `fe-gen emit` instead.
import { memo } from 'react';
import { useForm, useFieldArray } from 'react-hook-form';
import { Controller } from 'react-hook-form';
import Box from '@mui/material/Box';
import Divider from '@mui/material/Divider';
import IconButton from '@mui/material/IconButton';
import Stack from '@mui/material/Stack';
import FormControlLabel from '@mui/material/FormControlLabel';
import Switch from '@mui/material/Switch';
import CustomButton from 'common_remote/Button';
import CustomTextField from 'common_remote/TextField';
import CustomTypography from 'common_remote/Typography';
import { RefAutocompleteField } from '../../components/RefAutocompleteField';
import { useDebouncedSearch } from '../../hooks/useDebouncedSearch';
import CustomDialog from 'common_remote/Dialog';
import { searchIntentSpecification } from '../../service/intentSpecificationLookupService';
import { searchAssociationSpecification } from '../../service/associationSpecificationLookupService';

const SECTION_SX = {
  fontWeight: 700,
  color: '#2563eb',
  textTransform: 'uppercase' as const,
  fontSize: '0.72rem',
  letterSpacing: 0.8,
};

const ITEM_BOX_SX = {
  p: 1.5,
  border: '1px solid #e2e8f0',
  borderRadius: 1.5,
  bgcolor: '#f8fafc',
};

interface Props {
  open: boolean;
  onClose: () => void;
  onSave: (values: Record<string, any>) => Promise<void>;
  saving: boolean;
  /** Edit mode: prefill from an already-loaded detail record (mapDetailToDefaults) — the
   * dialog must remount per edit target (conditional render + a key on the target id) so
   * react-hook-form's defaultValues (captured once at mount) pick this up. Omit for create. */
  initialValues?: Record<string, any> | null;
  title?: string;
  submitLabel?: string;
}

function CreateIntentDialog({ open, onClose, onSave, saving, initialValues, title, submitLabel }: Readonly<Props>) {
  const { control, handleSubmit, reset } = useForm<Record<string, any>>({
    defaultValues: initialValues ?? {
  "description": "",
  "isBundle": false,
  "priority": "",
  "statusChangeDate": "",
  "context": "",
  "version": "",
  "intentSpecificationId": "",
  "intentRelationship": [],
  "characteristic": [],
  "relatedParty": [],
  "attachment": [],
  "name": "",
  "creationDate": "",
  "lastUpdate": "",
  "lifecycleStatus": ""
},
  });

  const { fields: intentRelationshipFields, append: addIntentRelationship, remove: removeIntentRelationship } = useFieldArray({ control, name: 'intentRelationship' });
  const { fields: characteristicFields, append: addCharacteristic, remove: removeCharacteristic } = useFieldArray({ control, name: 'characteristic' });
  const { fields: relatedPartyFields, append: addRelatedParty, remove: removeRelatedParty } = useFieldArray({ control, name: 'relatedParty' });
  const { fields: attachmentFields, append: addAttachment, remove: removeAttachment } = useFieldArray({ control, name: 'attachment' });
  const intentSpecificationIdSearch = useDebouncedSearch(searchIntentSpecification);
  const intentRelationshipAssociationSpecIdSearch = useDebouncedSearch(searchAssociationSpecification);

  const handleClose = () => {
    reset();
    onClose();
  };

  return (
    <CustomDialog
      open={open}
      onClose={handleClose}
      title={title ?? "Tambah Intent"}
      maxWidth="md"
      fullWidth
      content={
        <Box sx={{ pt: 1 }}>
          <Box sx={{ mb: 2 }}><Controller control={control} name={"description" as any} render={({ field: rhf }) => (
        <CustomTextField label="Deskripsi" value={rhf.value} onChange={rhf.onChange} multiline rows={3} />
      )} /></Box>
          <Box sx={{ mb: 2 }}><Controller control={control} name={"isBundle" as any} render={({ field: rhf }) => (
        <FormControlLabel control={<Switch checked={!!rhf.value} onChange={(e) => rhf.onChange(e.target.checked)} size="small" />} label="Is Bundle" />
      )} /></Box>
          <Box sx={{ mb: 2 }}><Controller control={control} name={"priority" as any} render={({ field: rhf }) => (
        <CustomTextField label="Priority" value={rhf.value} onChange={rhf.onChange} />
      )} /></Box>
          <Box sx={{ mb: 2 }}><Controller control={control} name={"statusChangeDate" as any} render={({ field: rhf }) => (
        <CustomTextField label="Status Change Date" value={rhf.value} onChange={rhf.onChange} />
      )} /></Box>
          <Box sx={{ mb: 2 }}><Controller control={control} name={"context" as any} render={({ field: rhf }) => (
        <CustomTextField label="Context" value={rhf.value} onChange={rhf.onChange} />
      )} /></Box>
          <Box sx={{ mb: 2 }}><Controller control={control} name={"version" as any} render={({ field: rhf }) => (
        <CustomTextField label="Version" value={rhf.value} onChange={rhf.onChange} />
      )} /></Box>
          <Box sx={{ mb: 2 }}><RefAutocompleteField control={control} idName={"intentSpecificationId"} label="Intent Specification ID" options={intentSpecificationIdSearch.options} loading={intentSpecificationIdSearch.loading} onSearch={intentSpecificationIdSearch.search} /></Box>
          <Box sx={{ mb: 2 }}><Controller control={control} name={"name" as any} render={({ field: rhf }) => (
        <CustomTextField label="Nama" value={rhf.value} onChange={rhf.onChange} />
      )} /></Box>
          <Box sx={{ mb: 2 }}><Controller control={control} name={"creationDate" as any} render={({ field: rhf }) => (
        <CustomTextField label="Creation Date" value={rhf.value} onChange={rhf.onChange} />
      )} /></Box>
          <Box sx={{ mb: 2 }}><Controller control={control} name={"lastUpdate" as any} render={({ field: rhf }) => (
        <CustomTextField label="Last Update" value={rhf.value} onChange={rhf.onChange} />
      )} /></Box>
          <Box sx={{ mb: 2 }}><Controller control={control} name={"lifecycleStatus" as any} render={({ field: rhf }) => (
        <CustomTextField label="Lifecycle Status" value={rhf.value} onChange={rhf.onChange} />
      )} /></Box>

          <Divider sx={{ my: 2.5 }} />

          <Stack direction="row" sx={{ mb: 1, justifyContent: 'space-between', alignItems: 'center' }}>
            <CustomTypography variant="subtitle2" sx={SECTION_SX}>
              "Intent Relationship" ({intentRelationshipFields.length})
            </CustomTypography>
            <CustomButton label="+ Tambah Intent Relationship" variant="outlined" onClick={() => addIntentRelationship({"atType":"EntityRelationship","name":"","role":"","relationshipType":"","associationSpecId":"","associationSpecName":"","associationSpecReferredType":""})} />
          </Stack>
          <Stack spacing={1.5}>
            {intentRelationshipFields.map((row, i) => (
              <Box key={row.id} sx={{ ...ITEM_BOX_SX, display: 'flex', flexWrap: 'wrap', gap: 1.5, alignItems: 'flex-start' }}>
                <Box sx={{ flex: 1, minWidth: 160 }}><Controller control={control} name={`intentRelationship.${i}.name` as any} render={({ field: rhf }) => (
                  <CustomTextField label="Nama" value={rhf.value} onChange={rhf.onChange} />
                )} /></Box>
                <Box sx={{ flex: 1, minWidth: 160 }}><Controller control={control} name={`intentRelationship.${i}.role` as any} render={({ field: rhf }) => (
                  <CustomTextField label="Role" value={rhf.value} onChange={rhf.onChange} />
                )} /></Box>
                <Box sx={{ flex: 1, minWidth: 160 }}><Controller control={control} name={`intentRelationship.${i}.relationshipType` as any} render={({ field: rhf }) => (
                  <CustomTextField label="Relationship Type" value={rhf.value} onChange={rhf.onChange} />
                )} /></Box>
                <Box sx={{ flex: 1, minWidth: 160 }}><RefAutocompleteField control={control} idName={`intentRelationship.${i}.associationSpecId`} nameName={`intentRelationship.${i}.associationSpecName`} label="Association Spec ID" options={intentRelationshipAssociationSpecIdSearch.options} loading={intentRelationshipAssociationSpecIdSearch.loading} onSearch={intentRelationshipAssociationSpecIdSearch.search} /></Box>
                <Box sx={{ flex: 1, minWidth: 160 }}><Controller control={control} name={`intentRelationship.${i}.associationSpecReferredType` as any} render={({ field: rhf }) => (
                  <CustomTextField label="@referredType" value={rhf.value} onChange={rhf.onChange} />
                )} /></Box>
                <IconButton size="small" onClick={() => removeIntentRelationship(i)} disabled={false} sx={{ mt: 0.5, color: '#ef4444' }}>✕</IconButton>
              </Box>
            ))}
          </Stack>

          <Divider sx={{ my: 2.5 }} />

          <Stack direction="row" sx={{ mb: 1, justifyContent: 'space-between', alignItems: 'center' }}>
            <CustomTypography variant="subtitle2" sx={SECTION_SX}>
              "Characteristic" ({characteristicFields.length})
            </CustomTypography>
            <CustomButton label="+ Tambah Characteristic" variant="outlined" onClick={() => addCharacteristic({"atType":"BooleanCharacteristic","name":"","valueType":""})} />
          </Stack>
          <Stack spacing={1.5}>
            {characteristicFields.map((row, i) => (
              <Box key={row.id} sx={{ ...ITEM_BOX_SX, display: 'flex', flexWrap: 'wrap', gap: 1.5, alignItems: 'flex-start' }}>
                <Box sx={{ flex: 1, minWidth: 160 }}><Controller control={control} name={`characteristic.${i}.name` as any} render={({ field: rhf }) => (
                  <CustomTextField label="Nama" value={rhf.value} onChange={rhf.onChange} />
                )} /></Box>
                <Box sx={{ flex: 1, minWidth: 160 }}><Controller control={control} name={`characteristic.${i}.valueType` as any} render={({ field: rhf }) => (
                  <CustomTextField label="Value Type" value={rhf.value} onChange={rhf.onChange} />
                )} /></Box>
                <IconButton size="small" onClick={() => removeCharacteristic(i)} disabled={false} sx={{ mt: 0.5, color: '#ef4444' }}>✕</IconButton>
              </Box>
            ))}
          </Stack>

          <Divider sx={{ my: 2.5 }} />

          <Stack direction="row" sx={{ mb: 1, justifyContent: 'space-between', alignItems: 'center' }}>
            <CustomTypography variant="subtitle2" sx={SECTION_SX}>
              "Related Party" ({relatedPartyFields.length})
            </CustomTypography>
            <CustomButton label="+ Tambah Related Party" variant="outlined" onClick={() => addRelatedParty({"atType":"RelatedPartyRefOrPartyRoleRef","role":""})} />
          </Stack>
          <Stack spacing={1.5}>
            {relatedPartyFields.map((row, i) => (
              <Box key={row.id} sx={{ ...ITEM_BOX_SX, display: 'flex', flexWrap: 'wrap', gap: 1.5, alignItems: 'flex-start' }}>
                <Box sx={{ flex: 1, minWidth: 160 }}><Controller control={control} name={`relatedParty.${i}.role` as any} render={({ field: rhf }) => (
                  <CustomTextField label="Role" value={rhf.value} onChange={rhf.onChange} />
                )} /></Box>
                <IconButton size="small" onClick={() => removeRelatedParty(i)} disabled={false} sx={{ mt: 0.5, color: '#ef4444' }}>✕</IconButton>
              </Box>
            ))}
          </Stack>

          <Divider sx={{ my: 2.5 }} />

          <Stack direction="row" sx={{ mb: 1, justifyContent: 'space-between', alignItems: 'center' }}>
            <CustomTypography variant="subtitle2" sx={SECTION_SX}>
              "Attachment" ({attachmentFields.length})
            </CustomTypography>
            <CustomButton label="+ Tambah Attachment" variant="outlined" onClick={() => addAttachment({"atType":"AttachmentRefOrValue"})} />
          </Stack>
          <Stack spacing={1.5}>
            {attachmentFields.map((row, i) => (
              <Box key={row.id} sx={{ ...ITEM_BOX_SX, display: 'flex', flexWrap: 'wrap', gap: 1.5, alignItems: 'flex-start' }}>

                <IconButton size="small" onClick={() => removeAttachment(i)} disabled={false} sx={{ mt: 0.5, color: '#ef4444' }}>✕</IconButton>
              </Box>
            ))}
          </Stack>

          <Divider sx={{ my: 2.5 }} />

        </Box>
      }
      actions={
        <Stack direction="row" spacing={1}>
          <CustomButton label="Cancel" variant="outlined" onClick={handleClose} />
          <CustomButton label={submitLabel ?? 'Create'} variant="contained" onClick={handleSubmit(onSave)} disabled={saving} />
        </Stack>
      }
    />
  );
}

export default memo(CreateIntentDialog);
