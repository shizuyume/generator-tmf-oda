// GENERATED by fe-gen emit mcs-common - DO NOT EDIT. Re-run `fe-gen emit` instead.
import { useCallback, useEffect, useState } from 'react';
import CustomBreadcrumbs from 'common_remote/Breadcrumbs';
import CustomButton from 'common_remote/Button';
import CustomDialog from 'common_remote/Dialog';
import CustomSnackbar from 'common_remote/Snackbar';
import CustomTypography from 'common_remote/Typography';
import Box from '@mui/material/Box';
import Stack from '@mui/material/Stack';
import Table from '@mui/material/Table';
import TableBody from '@mui/material/TableBody';
import TableCell from '@mui/material/TableCell';
import TableContainer from '@mui/material/TableContainer';
import TableHead from '@mui/material/TableHead';
import TableRow from '@mui/material/TableRow';
import PillTabs from 'common_remote/PillTabs';
import {
  DetailField,
  SectionCard,
  DETAIL_GRID_SX,
  ViewToggle,
  ViewMode,
} from '../../components/DetailComponents';
import CreateIntentDialog from './CreateIntentDialog';
import { buildPayload, mapDetailToDefaults } from '../../lib/buildPayload';
import type { ComputedFromRule } from '../../lib/buildPayload';
import { getIntent, deleteIntent, patchIntent, getErrorMessage } from '../../service';
import type { Intent } from '../../types';

type SnackState = { open: boolean; message: string; severity: 'success' | 'error' };

// TODO: CUSTOMIZE — per-resource template point (endpoint/fields differ per TMF component).
const EDIT_PAYLOAD_SPEC = {
  "description": "fields.description",
  "isBundle": "fields.isBundle",
  "priority": "fields.priority",
  "context": "fields.context",
  "intentSpecification": {
    "id": "fields.intentSpecificationId",
    "href": "fields.intentSpecificationHref"
  },
  "intentRelationship": "fields.intentRelationship",
  "characteristic": "fields.characteristic",
  "relatedParty": "fields.relatedParty",
  "attachment": "fields.attachment",
  "name": "fields.name",
  "lifecycleStatus": "fields.lifecycleStatus"
};
const EDIT_REPEATABLE_ITEM_PAYLOADS = {
  "intentRelationship": {
    "@type": "item.atType",
    "name": "item.name",
    "role": "item.role",
    "relationshipType": "item.relationshipType",
    "validFor": {
      "@type": "TimePeriod"
    },
    "associationSpec": {
      "@type": "EntityRef",
      "href": "item.associationSpecHref",
      "id": "item.associationSpecId",
      "name": "item.associationSpecName",
      "@referredType": "item.associationSpecReferredType"
    }
  },
  "characteristic": {
    "@type": "item.atType",
    "name": "item.name",
    "valueType": "item.valueType"
  },
  "relatedParty": {
    "@type": "item.atType",
    "role": "item.role"
  },
  "attachment": {
    "@type": "item.atType"
  }
};
const EDIT_COMPUTED_FROM_ROOT: ComputedFromRule[] = [
  {
    "target": "intentSpecificationHref",
    "source": "intentSpecificationId"
  }
];
const EDIT_COMPUTED_FROM_REPEATABLE: Record<string, ComputedFromRule[]> = {};

interface Props {
  id: string;
  onBack: () => void;
}

export default function IntentDetailPage({ id, onBack }: Readonly<Props>) {
  const [data, setData] = useState<Intent | null>(null);
  const [loading, setLoading] = useState(true);
  const [activeTab, setActiveTab] = useState(0);
  const [snackbar, setSnackbar] = useState<SnackState>({ open: false, message: '', severity: 'success' });
  const [confirmDelete, setConfirmDelete] = useState(false);
  const [editOpen, setEditOpen] = useState(false);
  const [savingEdit, setSavingEdit] = useState(false);
  const [intentRelationshipView, setIntentRelationshipView] = useState<ViewMode>('card');
  const [characteristicView, setCharacteristicView] = useState<ViewMode>('card');
  const [relatedPartyView, setRelatedPartyView] = useState<ViewMode>('card');
  const [attachmentView, setAttachmentView] = useState<ViewMode>('card');

  const loadDetail = useCallback(async () => {
    setLoading(true);
    try {
      const result = await getIntent(id);
      setData(result);
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    } finally {
      setLoading(false);
    }
  }, [id]);

  useEffect(() => { loadDetail(); }, [loadDetail]);

  const handleDelete = async () => {
    try {
      await deleteIntent(id);
      setSnackbar({ open: true, message: 'Deleted successfully', severity: 'success' });
      setConfirmDelete(false);
      onBack();
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    }
  };
  const handleEditSave = async (values: Record<string, any>) => {
    setSavingEdit(true);
    try {
      const payload = buildPayload(values, EDIT_PAYLOAD_SPEC, EDIT_REPEATABLE_ITEM_PAYLOADS, EDIT_COMPUTED_FROM_ROOT, EDIT_COMPUTED_FROM_REPEATABLE);
      await patchIntent(id, payload as any);
      setSnackbar({ open: true, message: 'Saved successfully', severity: 'success' });
      setEditOpen(false);
      await loadDetail();
    } catch (err) {
      setSnackbar({ open: true, message: getErrorMessage(err), severity: 'error' });
    } finally {
      setSavingEdit(false);
    }
  };

  if (loading) {
    return <Box sx={{ p: 3 }}><CustomTypography>Loading...</CustomTypography></Box>;
  }
  if (!data) {
    return (
      <Box sx={{ p: 3 }}>
        <CustomButton label="← Back" variant="text" onClick={onBack} />
        <CustomTypography color="error">Not found</CustomTypography>
      </Box>
    );
  }

  const tab0Content = (
    <>
      <SectionCard>
          <Box sx={DETAIL_GRID_SX}>
            <DetailField label={"Deskripsi"} value={data.description} />
            <DetailField label={"Is Bundle"} value={data.isBundle} />
            <DetailField label={"Priority"} value={data.priority} />
            <DetailField label={"Status Change Date"} value={data.statusChangeDate} />
            <DetailField label={"Context"} value={data.context} />
            <DetailField label={"Version"} value={data.version} />
            <DetailField label={"Nama"} value={data.name} />
            <DetailField label={"Creation Date"} value={data.creationDate} />
            <DetailField label={"Last Update"} value={data.lastUpdate} />
            <DetailField label={"Lifecycle Status"} value={data.lifecycleStatus} />
            <DetailField label={"@type"} value={data["@type"]} />
            <DetailField label={"ID"} value={data.id} />
            <DetailField label={"Href"} value={data.href ? <a href={data.href}>{data.href}</a> : undefined} />
          </Box>
        </SectionCard>
    </>
  );

  const tab1Content = (
    <>
      {(() => { const intentRelationshipItems = data.intentRelationship ?? []; return (
        <SectionCard title={"Intent Relationship"} action={<ViewToggle value={intentRelationshipView} onChange={setIntentRelationshipView} />}>
          {(intentRelationshipItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada Intent Relationship</CustomTypography>
          ) : intentRelationshipView === 'card' ? (
            <Stack spacing={1.5}>
              {(intentRelationshipItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"name"} value={item["name"]} />
                    <DetailField label={"role"} value={item["role"]} />
                    <DetailField label={"relationshipType"} value={item["relationshipType"]} />
                    <DetailField label={"Valid For"} value={item["validFor"]?.["name"]} />
                    <DetailField label={"Association Spec"} value={item["associationSpec"]?.["name"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>name</TableCell>
                    <TableCell>role</TableCell>
                    <TableCell>relationshipType</TableCell>
                    <TableCell>Valid For</TableCell>
                    <TableCell>Association Spec</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(intentRelationshipItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["name"] ?? '-'}</TableCell>
                      <TableCell>{row["role"] ?? '-'}</TableCell>
                      <TableCell>{row["relationshipType"] ?? '-'}</TableCell>
                      <TableCell>{row["validFor"]?.["name"] ?? '-'}</TableCell>
                      <TableCell>{row["associationSpec"]?.["name"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tab2Content = (
    <>
      {(() => { const characteristicItems = data.characteristic ?? []; return (
        <SectionCard title={"Characteristic"} action={<ViewToggle value={characteristicView} onChange={setCharacteristicView} />}>
          {(characteristicItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada Characteristic</CustomTypography>
          ) : characteristicView === 'card' ? (
            <Stack spacing={1.5}>
              {(characteristicItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"name"} value={item["name"]} />
                    <DetailField label={"valueType"} value={item["valueType"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>name</TableCell>
                    <TableCell>valueType</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(characteristicItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["name"] ?? '-'}</TableCell>
                      <TableCell>{row["valueType"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tab3Content = (
    <>
      {(() => { const relatedPartyItems = data.relatedParty ?? []; return (
        <SectionCard title={"Related Party"} action={<ViewToggle value={relatedPartyView} onChange={setRelatedPartyView} />}>
          {(relatedPartyItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada Related Party</CustomTypography>
          ) : relatedPartyView === 'card' ? (
            <Stack spacing={1.5}>
              {(relatedPartyItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>
                    <DetailField label={"role"} value={item["role"]} />
                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>
                    <TableCell>role</TableCell>
                  </TableRow>
                </TableHead>
                <TableBody>
                  {(relatedPartyItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>
                      <TableCell>{row["role"] ?? '-'}</TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tab4Content = (
    <>
      {(() => { const attachmentItems = data.attachment ?? []; return (
        <SectionCard title={"Attachment"} action={<ViewToggle value={attachmentView} onChange={setAttachmentView} />}>
          {(attachmentItems).length === 0 ? (
            <CustomTypography variant="body2" color="text.secondary">Belum ada Attachment</CustomTypography>
          ) : attachmentView === 'card' ? (
            <Stack spacing={1.5}>
              {(attachmentItems).map((item: any, idx: number) => (
                <Box key={item.id ?? idx} sx={{ p: 2, border: '1px solid #e2e8f0', borderRadius: 2, bgcolor: '#f8fafc' }}>
                  <Box sx={DETAIL_GRID_SX}>

                  </Box>
                </Box>
              ))}
            </Stack>
          ) : (
            <TableContainer>
              <Table size="small">
                <TableHead>
                  <TableRow>

                  </TableRow>
                </TableHead>
                <TableBody>
                  {(attachmentItems).map((row: any, idx: number) => (
                    <TableRow key={row.id ?? idx}>

                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </TableContainer>
          )}
        </SectionCard>
      ); })()}
    </>
  );

  const tabs = [
    { label: "Ringkasan", content: tab0Content },
    { label: "Intent Relationship", content: tab1Content },
    { label: "Characteristic", content: tab2Content },
    { label: "Related Party", content: tab3Content },
    { label: "Attachment", content: tab4Content }
  ];

  return (
    <Box sx={{ p: 3 }}>
      <CustomBreadcrumbs items={[{ label: "Intent", onClick: onBack }, { label: (data as any).name ?? data.id }]} />

      <Stack direction="row" spacing={1.25} sx={{ mt: 2 }}>
        <CustomButton label="← Back to List" onClick={onBack} variant="outlined" />
        <CustomButton label="Edit" onClick={() => setEditOpen(true)} variant="contained" />
        <CustomButton label="Delete" onClick={() => setConfirmDelete(true)} variant="contained" color="error" />
      </Stack>

      <Box sx={{ mt: 2, mb: 1 }}>
        <CustomTypography variant="h5">{(data as any).name ?? data.id}</CustomTypography>
      </Box>

      <Box sx={{ mt: 2 }}>
        <PillTabs tabs={tabs} value={activeTab} onChange={setActiveTab} />
      </Box>

      <CustomDialog
        open={confirmDelete}
        onClose={() => setConfirmDelete(false)}
        title="Delete"
        content={<CustomTypography>Are you sure you want to delete this record?</CustomTypography>}
        actions={
          <Stack direction="row" spacing={1}>
            <CustomButton label="Cancel" variant="outlined" onClick={() => setConfirmDelete(false)} />
            <CustomButton label="Delete" variant="contained" color="error" onClick={handleDelete} />
          </Stack>
        }
      />

      {editOpen && (
        <CreateIntentDialog
          open={editOpen}
          onClose={() => setEditOpen(false)}
          onSave={handleEditSave}
          saving={savingEdit}
          initialValues={mapDetailToDefaults(data as unknown as Record<string, any>, EDIT_PAYLOAD_SPEC, EDIT_REPEATABLE_ITEM_PAYLOADS)}
          title="Edit Intent"
          submitLabel="Save"
        />
      )}

      <CustomSnackbar open={snackbar.open} onClose={() => setSnackbar((s) => ({ ...s, open: false }))} message={snackbar.message} severity={snackbar.severity} />
    </Box>
  );
}
