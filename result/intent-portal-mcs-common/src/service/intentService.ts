// GENERATED by fe-gen emit mcs-common - DO NOT EDIT. Re-run `fe-gen emit` instead.
import { apiGet, apiGetList, apiPost, apiPatch, apiDelete } from './api';
import type { Intent, Intent_FVO, Intent_MVO } from '../types';
import type { ListParams } from '../types';

const ENDPOINT = "/intent";

export async function listIntents(params?: ListParams) {
  return apiGetList<Intent>(ENDPOINT, params);
}

export async function getIntent(id: string, fields?: string) {
  return apiGet<Intent>(`${ENDPOINT}/${id}`, fields ? { fields } : undefined);
}

export async function createIntent(body: Intent_FVO) {
  return apiPost<Intent>(ENDPOINT, body);
}

export async function patchIntent(id: string, body: Intent_MVO) {
  return apiPatch<Intent>(`${ENDPOINT}/${id}`, body);
}

export async function deleteIntent(id: string) {
  return apiDelete(`${ENDPOINT}/${id}`);
}
